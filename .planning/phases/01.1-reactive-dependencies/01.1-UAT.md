---
status: testing
phase: 01.1-reactive-dependencies
source: [01.1-01-SUMMARY.md, 01.1-02-SUMMARY.md]
started: 2026-03-27T17:10:00Z
updated: 2026-03-27T17:10:00Z
---

## Current Test

number: 1
name: Add runs installer without pre-installing deps
expected: |
  Run `cellar add` with a game that has a recipe with setup_deps (e.g., Cossacks with dotnet48).
  The installer should start immediately — no winetricks dependency installation before the installer runs.
  Previously this would spend 30+ minutes installing dotnet48 upfront.
awaiting: user response

## Tests

### 1. Add runs installer without pre-installing deps
expected: Run `cellar add` with a recipe that has setup_deps. Installer runs immediately without winetricks pre-install. No 30+ minute dotnet48 wait.
result: [pending]

### 2. Reactive dep install on installer failure
expected: If the installer fails with a diagnosable Wine error (e.g., missing DLL mapped to a winetricks verb), WinetricksRunner auto-installs the dep and retries the installer without user intervention.
result: [pending]

### 3. Fallback prompt for undiagnosed failure
expected: If installer fails with no specific diagnosis but recipe has setup_deps, user is prompted whether to install the recipe's known deps. User can accept or decline.
result: [pending]

### 4. --force-proactive-deps flag
expected: Running `cellar add --force-proactive-deps` pre-installs all recipe setup_deps before running the installer (old behavior). Flag exists and is accepted by the CLI.
result: [pending]

### 5. Launch retries same config after dep install
expected: If `cellar launch` fails and a dep is installed, it retries the same Wine config (not cycling to next variant). Only advances to next variant when dep install didn't help.
result: [pending]

### 6. Winetricks stale-output timeout
expected: If a winetricks install hangs (no output for 5 minutes), the process is killed and the result reports a timeout rather than hanging indefinitely.
result: [pending]

### 7. Launch attempt cap at 5
expected: Launch retry loop stops after 5 total attempts (across both dep installs and variant cycling), producing an exhaustion report rather than looping forever.
result: [pending]

## Summary

total: 7
passed: 0
issues: 0
pending: 7
skipped: 0

## Gaps

[none yet]
