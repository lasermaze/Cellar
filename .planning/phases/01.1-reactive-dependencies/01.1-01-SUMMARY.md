---
phase: 01.1-reactive-dependencies
plan: 01
subsystem: core
tags: [winetricks, wine, process, timeout, stale-output, NSLock, Sendable]

# Dependency graph
requires:
  - phase: 01-cossacks-launches
    provides: WineProcess with StderrCapture/NSLock pattern, killWineserver()
provides:
  - WinetricksRunner service with stale-output timeout protection
  - WineResult.timedOut field for timeout-aware result handling
  - readabilityHandler cleanup after drain in WineProcess (prevents spurious callbacks)
affects:
  - 01.1-reactive-dependencies (subsequent plans building on WinetricksRunner)
  - AddCommand (will be refactored to use WinetricksRunner in later plan)
  - LaunchCommand (will use WinetricksRunner for reactive dep install)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "OutputMonitor: NSLock + @unchecked Sendable timestamp wrapper for thread-safe stale-output detection"
    - "Stale-output polling loop: Thread.sleep(2s) polling instead of waitUntilExit() for timeout control"
    - "Post-exit drain + nil handler pattern: readDataToEndOfFile() then readabilityHandler = nil"

key-files:
  created:
    - Sources/cellar/Core/WinetricksRunner.swift
  modified:
    - Sources/cellar/Models/WineResult.swift
    - Sources/cellar/Core/WineProcess.swift
    - Sources/cellar/Commands/AddCommand.swift

key-decisions:
  - "WinetricksRunner always passes -q to winetricks for unattended (no-dialog) mode"
  - "Stale-output timeout is 5 minutes — no output for 5 min = process assumed hung, kill it"
  - "Timeout cleanup kills process.terminate() then wineserver to clean up all Wine child processes"
  - "OutputMonitor uses same NSLock + @unchecked Sendable pattern as WineProcess.StderrCapture for Swift 6 compliance"

patterns-established:
  - "OutputMonitor pattern: thread-safe Date wrapper with NSLock for stale-output detection"
  - "Stale-output polling replaces waitUntilExit() when timeout detection is needed"
  - "readabilityHandler = nil after drain: applied consistently in WineProcess and WinetricksRunner"

requirements-completed: [AGENT-02]

# Metrics
duration: 2min
completed: 2026-03-27
---

# Phase 01.1 Plan 01: WinetricksRunner Service Summary

**WinetricksRunner service with 5-minute stale-output timeout, WineResult.timedOut field, and WineProcess drain/nil-handler cleanup**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-03-27T16:57:28Z
- **Completed:** 2026-03-27T16:59:05Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Created WinetricksRunner.swift with OutputMonitor (NSLock Sendable), install(verb:), and stale-output polling loop
- Extended WineResult with timedOut: Bool field; WineProcess.run() returns timedOut: false
- Added readabilityHandler = nil after drain in WineProcess.run() and initPrefix() (prevents spurious callbacks)
- Fixed AddCommand's redundant double-nil-check (collapsed to single-line optional binding)

## Task Commits

1. **Task 1: Add timedOut to WineResult and fix WineProcess drain** - `8578f2d` (feat)
2. **Task 2: Create WinetricksRunner service with stale-output timeout** - `8bae993` (feat)

## Files Created/Modified

- `Sources/cellar/Core/WinetricksRunner.swift` - New reusable service: WinetricksResult struct, WinetricksRunner.install(verb:) with stale-output detection
- `Sources/cellar/Models/WineResult.swift` - Added timedOut: Bool field after elapsed
- `Sources/cellar/Core/WineProcess.swift` - readabilityHandler = nil after drain in run() and initPrefix(); timedOut: false in WineResult return
- `Sources/cellar/Commands/AddCommand.swift` - Collapsed redundant nil check to single-line optional binding

## Decisions Made

- WinetricksRunner always passes `-q` for unattended mode (no dialog boxes during install)
- 5-minute stale-output timeout: if no output produced for 5 min the process is assumed hung
- Timeout cleanup: process.terminate() followed by wineserver kill to ensure Wine child processes are cleaned up
- OutputMonitor reuses the NSLock + @unchecked Sendable pattern from WineProcess.StderrCapture for Swift 6 compliance

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- WinetricksRunner is ready to use in AddCommand and LaunchCommand for reactive dependency installation
- WineResult.timedOut is available for timeout-aware result handling in the retry loop
- Foundation for Phase 01.1 Plan 02 (AddCommand refactor to use WinetricksRunner)

---
*Phase: 01.1-reactive-dependencies*
*Completed: 2026-03-27*
