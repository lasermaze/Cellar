---
phase: 32-middleware-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/cellar/Core/AgentMiddleware.swift
autonomous: true
requirements:
  - MW-01
  - MW-02
  - MW-03

must_haves:
  truths:
    - "AgentMiddleware protocol defines beforeTool, afterTool, afterStep hooks"
    - "BudgetTracker emits warnings at 50% and 80% budget thresholds"
    - "SpinDetector detects 2-tool repeating cycles and same-tool-4x patterns"
    - "MiddlewareContext holds shared state readable by all middleware"
  artifacts:
    - path: "Sources/cellar/Core/AgentMiddleware.swift"
      provides: "Middleware protocol, context, BudgetTracker, SpinDetector"
      contains: "protocol AgentMiddleware"
  key_links:
    - from: "AgentMiddleware.swift"
      to: "AgentLoop.swift"
      via: "ToolResult type used by beforeTool return"
      pattern: "ToolResult\\?"
    - from: "AgentMiddleware.swift"
      to: "AgentControl.swift"
      via: "MiddlewareContext.control property"
      pattern: "let control: AgentControl"
---

<objective>
Create the AgentMiddleware protocol, MiddlewareContext shared state class, and two concrete middleware implementations (BudgetTracker, SpinDetector) in a new file.

Purpose: Extract budget tracking and spin detection from the monolithic loop body into composable, testable middleware units. These compile standalone without modifying any existing files.
Output: Sources/cellar/Core/AgentMiddleware.swift
</objective>

<execution_context>
@/Users/peter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/32-middleware-system/32-CONTEXT.md

<interfaces>
<!-- Phase 31 types used by this plan -->

From Sources/cellar/Core/AgentLoop.swift:
```swift
enum ToolResult: Sendable {
    case success(content: String)
    case stop(content: String, reason: StopReason)
    case error(content: String)
    enum StopReason: Sendable { case userAborted; case userConfirmedWorking }
    var content: String { ... }
    var isStop: Bool { ... }
    var isError: Bool { ... }
}

enum AgentEvent: Sendable {
    case budgetWarning(percentage: Int)
    case status(String)
    // ... other cases
}
```

From Sources/cellar/Core/AgentControl.swift:
```swift
final class AgentControl: Sendable {
    var shouldAbort: Bool { ... }
    var userForceConfirmed: Bool { ... }
    func abort() { ... }
    func confirm() { ... }
}
```

From Sources/cellar/Models/AIModels.swift:
```swift
indirect enum JSONValue: Codable, Equatable {
    case string(String)
    case number(Double)
    case bool(Bool)
    case array([JSONValue])
    case object([String: JSONValue])
    case null
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AgentMiddleware protocol and MiddlewareContext</name>
  <files>Sources/cellar/Core/AgentMiddleware.swift</files>
  <action>
Create new file Sources/cellar/Core/AgentMiddleware.swift with:

1. `import Foundation` at top.

2. `protocol AgentMiddleware` with three hooks — use exact signatures from the brief:
   - `func beforeTool(name: String, input: JSONValue, context: MiddlewareContext) -> ToolResult?` — return nil to proceed, or a ToolResult to short-circuit
   - `func afterTool(name: String, input: JSONValue, result: ToolResult, context: MiddlewareContext)` — observe results
   - `func afterStep(context: MiddlewareContext) -> String?` — return optional message to inject as user message

3. `final class MiddlewareContext` — shared mutable context passed by reference to all middleware:
   - `let control: AgentControl`
   - `var iterationCount: Int = 0`
   - `var estimatedCost: Double = 0`
   - `var budgetCeiling: Double = 5.0`
   - `var recentActionTools: [String] = []`
   - Flags set by middleware, read by loop:
     - `var shouldInjectBudgetWarning = false`
     - `var budgetWarningMessage: String? = nil`
     - `var shouldInjectPivotNudge = false`
     - `var pivotNudgeMessage: String? = nil`
   - `init(control: AgentControl, budgetCeiling: Double)` — sets control and budgetCeiling

This task creates the file with protocol + context only. BudgetTracker and SpinDetector are added in Task 2.
  </action>
  <verify>
    <automated>cd /Users/peter/Documents/Cellar && swift build 2>&1 | tail -5</automated>
  </verify>
  <done>AgentMiddleware.swift exists with protocol and MiddlewareContext class, project compiles</done>
</task>

<task type="auto">
  <name>Task 2: Implement BudgetTracker and SpinDetector middleware</name>
  <files>Sources/cellar/Core/AgentMiddleware.swift</files>
  <action>
Add two concrete middleware classes to AgentMiddleware.swift below the MiddlewareContext class:

1. `final class BudgetTracker: AgentMiddleware` — exact implementation from brief:
   - Private state: `hasAlertedAt50 = false`, `hasWarnedAt80 = false`, `hasSentWarningMessage = false`
   - `let emit: (AgentEvent) -> Void` callback
   - `init(emit: @escaping (AgentEvent) -> Void)`
   - `beforeTool`: return nil (budget doesn't block individual tools)
   - `afterTool`: empty (no per-tool action)
   - `afterStep`: compute fraction = estimatedCost / budgetCeiling. At >=50% and !hasAlertedAt50: set flag, emit .budgetWarning(percentage: 50). At >=80% and !hasSentWarningMessage: set both flags, return the budget warning string: `"[BUDGET WARNING: {pct}% of session budget used (${cost} / ${ceiling}). Begin wrapping up - save progress and finalize results soon.]"`. Otherwise return nil.

2. `final class SpinDetector: AgentMiddleware` — exact implementation from brief:
   - Private state: `hasSentNudge = false`
   - `let actionTools: Set<String>` initialized to `["set_environment", "set_registry", "install_winetricks", "place_dll", "write_game_file", "launch_game"]`
   - `let emit: (AgentEvent) -> Void` callback
   - `init(emit: @escaping (AgentEvent) -> Void)`
   - `beforeTool`: return nil
   - `afterTool`: if tool name is in actionTools, append to context.recentActionTools (cap at 8 by removing first)
   - `afterStep`: guard !hasSentNudge and recentActionTools.count >= 6. Check last 6 for 2-tool repeating cycle (A-B-A-B-A-B) OR same tool 4+ times. If detected: set hasSentNudge, emit .status("[Spin detected — pivot nudge injected]"), return the pivot nudge string from the brief.
  </action>
  <verify>
    <automated>cd /Users/peter/Documents/Cellar && swift build 2>&1 | tail -5</automated>
  </verify>
  <done>BudgetTracker and SpinDetector compile, implement AgentMiddleware protocol with exact behavior from brief</done>
</task>

</tasks>

<verification>
1. `swift build` completes with no errors
2. AgentMiddleware.swift contains: protocol AgentMiddleware, class MiddlewareContext, class BudgetTracker, class SpinDetector
3. No modifications to any existing files (AgentLoop.swift, AgentTools.swift, etc.)
</verification>

<success_criteria>
- AgentMiddleware protocol with beforeTool/afterTool/afterStep hooks compiles
- MiddlewareContext references AgentControl from Phase 31
- BudgetTracker fires at 50% and 80% thresholds
- SpinDetector detects 2-tool cycles and same-tool-4x patterns
- All new code in a single new file, no existing files modified
</success_criteria>

<output>
After completion, create `.planning/phases/32-middleware-system/32-01-SUMMARY.md`
</output>
