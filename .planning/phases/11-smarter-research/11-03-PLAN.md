---
phase: 11-smarter-research
plan: 03
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - Sources/cellar/Core/AIService.swift
autonomous: true
requirements:
  - RSRCH-01
  - RSRCH-02

must_haves:
  truths:
    - "System prompt instructs the agent to check extracted_fixes first before reading raw text_content"
    - "System prompt documents the similar_games query type for cross-game solution matching"
    - "Research Quality methodology section is placed between Dialog Detection and macOS + Wine Domain Knowledge"
  artifacts:
    - path: "Sources/cellar/Core/AIService.swift"
      provides: "Research Quality methodology in agent system prompt"
      contains: "Research Quality"
  key_links:
    - from: "Sources/cellar/Core/AIService.swift"
      to: "Sources/cellar/Core/AgentTools.swift"
      via: "System prompt references fetch_page extracted_fixes and query_successdb similar_games"
      pattern: "extracted_fixes"
---

<objective>
Add a Research Quality methodology section to the agent system prompt that teaches the agent how to use structured fix extraction from fetch_page and cross-game similarity queries from query_successdb.

Purpose: The agent needs explicit guidance to prefer extracted_fixes over raw text and to use similar_games queries when no exact game match exists in the success database.
Output: Updated AIService.swift system prompt with Research Quality section.
</objective>

<execution_context>
@/Users/peter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-smarter-research/11-CONTEXT.md

<interfaces>
<!-- From AIService.swift system prompt section ordering (current) -->
## Three-Phase Workflow: Research -> Diagnose -> Adapt
## Engine-Aware Methodology
## Dialog Detection
## macOS + Wine Domain Knowledge     <!-- Research Quality goes BEFORE this -->
## Available Tools (19 total)
## Constraints
## Communication

<!-- From fetch_page result (Plan 02) -->
{
  "url": "...",
  "text_content": "...",           // SwiftSoup-extracted meaningful text
  "extracted_fixes": {              // NEW: structured fix data
    "env_vars": [{"name": "...", "value": "...", "context": "..."}],
    "registry": [{"path": "...", "value": "...", "context": "..."}],
    "dlls": [{"name": "...", "mode": "...", "context": "..."}],
    "winetricks": [{"verb": "...", "context": "..."}],
    "ini_changes": [{"file": "...", "key": "...", "value": "...", "context": "..."}]
  },
  "length": 1234,
  "truncated": false
}

<!-- From query_successdb similar_games (Plan 02) -->
query_successdb({ "similar_games": { "engine": "...", "graphics_api": "...", "tags": [...], "symptom": "..." } })
// Returns: { "query_type": "similar_games", "matches": [{ ...record, "similarity_score": N }] }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Research Quality methodology to system prompt</name>
  <files>Sources/cellar/Core/AIService.swift</files>
  <action>
Add a new `## Research Quality` section to the system prompt in AIService.swift. Place it between the `## Dialog Detection` section and the `## macOS + Wine Domain Knowledge` section, following the same pattern used for Engine-Aware Methodology (Phase 9) and Dialog Detection (Phase 10).

**New section content:**

```
## Research Quality

fetch_page returns structured data — use it effectively.

### Using extracted_fixes (after fetch_page)

1. Check the `extracted_fixes` field FIRST — it contains specific, actionable fixes already parsed from the page
2. Apply extracted fixes directly when confident:
   - `env_vars`: Set via configure_wine environment parameter
   - `dlls`: Set via configure_wine dll_overrides parameter
   - `registry`: Set via configure_wine registry parameter
   - `winetricks`: Install via install_dependency
   - `ini_changes`: Write via write_file
3. Fall back to `text_content` only when extracted_fixes is empty or when you need additional context to understand WHY a fix works
4. Each extracted fix includes a `context` field showing its source — use this to assess credibility

### Cross-Game Solution Matching

When query_successdb returns no results for game_id, try similar_games:

```
query_successdb({
  "similar_games": {
    "engine": "<detected engine>",
    "graphics_api": "<detected API>",
    "tags": ["<relevant tags>"],
    "symptom": "<current symptom>"
  }
})
```

Results are ranked by signal overlap:
- Engine match (strongest signal) — same engine family likely needs same renderer config
- Graphics API match — same API means same DLL override patterns
- Tag overlap — genre/era similarity suggests common issues
- Symptom match — similar failure modes suggest similar fixes

Apply fixes from high-similarity matches (score 4+) with confidence. For lower scores, use as research hints for web search queries.

### Research Workflow Integration

In Phase 1 Research:
1. query_successdb with game_id first
2. If no exact match, query_successdb with similar_games using engine + graphics_api from inspect_game
3. search_web for game-specific fixes
4. fetch_page on promising results — check extracted_fixes before reading text_content
5. Combine extracted fixes with similar-game solutions to build initial configuration
```

**Also update the Phase 1 Research section** (under Three-Phase Workflow) to add a step referencing the Research Quality methodology. After existing step 2b (engine detection checkpoint), add:

```
2c. If no exact successdb match, query similar_games with engine and graphics_api — apply high-confidence fixes from similar games (see Research Quality below)
```

**Also update the Available Tools section** to reflect the enhanced fetch_page and query_successdb descriptions:
- fetch_page: Update description to mention "returns structured extracted_fixes with env vars, DLLs, registry paths, winetricks verbs, and INI changes alongside text content"
- query_successdb: Update description to mention "supports similar_games composite query for cross-game solution matching by engine, graphics API, tags, and symptoms"
  </action>
  <verify>
    <automated>cd /Users/peter/Documents/Cellar && swift build 2>&1 | tail -5</automated>
  </verify>
  <done>System prompt contains Research Quality section between Dialog Detection and Domain Knowledge. Phase 1 Research includes step 2c for similar_games fallback. Tool descriptions updated for fetch_page and query_successdb.</done>
</task>

</tasks>

<verification>
1. `swift build` compiles without errors
2. System prompt contains `## Research Quality` section
3. Research Quality section covers: extracted_fixes usage, cross-game matching, workflow integration
4. Phase 1 Research has step 2c for similar_games
5. Available Tools descriptions updated for fetch_page and query_successdb
6. Section ordering: Dialog Detection > Research Quality > macOS + Wine Domain Knowledge
</verification>

<success_criteria>
The agent system prompt teaches the agent to use extracted_fixes from fetch_page before raw text, to query similar_games for cross-game solutions, and integrates research quality into the existing Research-Diagnose-Adapt workflow. The section is correctly placed in the prompt hierarchy.
</success_criteria>

<output>
After completion, create `.planning/phases/11-smarter-research/11-03-SUMMARY.md`
</output>
