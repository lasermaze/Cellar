---
phase: 07-agentic-v2-research-diagnose-adapt-loop-with-web-search-diagnostic-traces-and-success-database
plan: 05
type: execute
wave: 3
depends_on: ["07-02", "07-03", "07-04"]
files_modified:
  - Sources/cellar/Core/AgentTools.swift
  - Sources/cellar/Core/AIService.swift
autonomous: true
requirements: []

must_haves:
  truths:
    - "Agent can search the web for game-specific Wine compatibility info"
    - "Agent can fetch and extract text from a specific URL"
    - "Research results are cached per game with 7-day TTL"
    - "System prompt guides agent through Research-Diagnose-Adapt workflow with correct macOS/Wine domain knowledge"
    - "launch_game returns structured DLL load analysis and sets CWD correctly"
  artifacts:
    - path: "Sources/cellar/Core/AgentTools.swift"
      provides: "search_web, fetch_page tools + enhanced launch_game"
      contains: "search_web"
    - path: "Sources/cellar/Core/AIService.swift"
      provides: "Updated system prompt with v2 workflow, domain knowledge, and all tool references"
      contains: "Research phase"
  key_links:
    - from: "Sources/cellar/Core/AIService.swift"
      to: "Sources/cellar/Core/AgentTools.swift"
      via: "System prompt references all tools including new v2 tools; toolDefinitions array passed to AgentLoop"
      pattern: "AgentTools.toolDefinitions"
    - from: "Sources/cellar/Core/AgentTools.swift (search_web)"
      to: "Sources/cellar/Persistence/CellarPaths.swift"
      via: "Research cache stored at CellarPaths.researchCacheFile"
      pattern: "researchCacheFile"
---

<objective>
Add research tools (search_web, fetch_page), enhance launch_game with structured output, and update the system prompt for the Research-Diagnose-Adapt workflow.

Purpose: This is the capstone plan that gives the agent web research capability, enhances launch_game to return diagnostic data, and rewrites the system prompt to guide the agent through the three-phase Research-Diagnose-Adapt workflow with correct macOS/Wine domain knowledge. After this plan, the v2 agent architecture is complete.
Output: Two new research tools, enhanced launch_game, and updated system prompt in AIService.
</objective>

<execution_context>
@/Users/peter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Sources/cellar/Core/AgentTools.swift
@Sources/cellar/Core/AIService.swift
@Sources/cellar/Persistence/CellarPaths.swift

<interfaces>
From Sources/cellar/Core/AIService.swift:
```swift
static func runAgentLoop(gameId:entry:executablePath:wineURL:bottleURL:wineProcess:) -> AIResult<String>
// System prompt is a string literal inside runAgentLoop
// Uses AgentTools.toolDefinitions and agentLoop.run()
```

From Sources/cellar/Persistence/CellarPaths.swift (after plan 07-01):
```swift
static let researchCacheDir: URL
static func researchCacheFile(for gameId: String) -> URL
```

From Sources/cellar/Core/AgentTools.swift (after plans 01-04):
```swift
// execute() switch now has: inspect_game, read_log, read_registry, ask_user,
// set_environment, set_registry, install_winetricks, place_dll, launch_game,
// save_recipe, write_game_file, trace_launch, check_file_access,
// verify_dll_override, query_successdb, save_success
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: search_web + fetch_page tools with research cache</name>
  <files>Sources/cellar/Core/AgentTools.swift</files>
  <action>
1. Add `search_web` and `fetch_page` to `execute()` switch and `toolDefinitions`.

2. **Research cache Codable struct** (add at top of AgentTools.swift or as private inner type):
   ```swift
   private struct ResearchCache: Codable {
       let gameId: String
       let fetchedAt: String  // ISO8601
       let results: [ResearchResult]

       func isStale() -> Bool {
           // Stale if > 7 days old
           let formatter = ISO8601DateFormatter()
           guard let date = formatter.date(from: fetchedAt) else { return true }
           return Date().timeIntervalSince(date) > 7 * 24 * 3600
       }
   }
   private struct ResearchResult: Codable {
       let source: String  // "winehq", "pcgamingwiki", "duckduckgo"
       let url: String
       let title: String
       let snippet: String
   }
   ```

3. **search_web ToolDefinition:**
   - name: "search_web"
   - description: "Search the web for game-specific Wine compatibility info. Targets WineHQ, ProtonDB, PCGamingWiki, and forums. Results are cached per game for 7 days. Returns structured snippets, not full pages — use fetch_page to read a specific URL. Call this after checking query_successdb."
   - Required param: `query` (string)

4. **search_web implementation** (`private func searchWeb(input: JSONValue) -> String`):
   - Extract query string
   - Check research cache: load from CellarPaths.researchCacheFile(for: gameId), decode ResearchCache, check isStale(). If valid cache exists, return cached results with `from_cache: true`.
   - If no cache or stale: use DuckDuckGo HTML search (no API key needed):
     - URL: `https://html.duckduckgo.com/html/?q=\(encodedQuery)+wine+compatibility`
     - Use DispatchSemaphore + ResultBox pattern (same as AgentLoop.callAPI)
     - Set User-Agent header to "Mozilla/5.0 (Macintosh; Intel Mac OS X 10_15_7)"
     - Parse HTML response: extract result titles and snippets using simple regex:
       - Result links: scan for `<a rel="nofollow" class="result__a" href="([^"]+)"`
       - Titles: content between `<a...class="result__a"...>` and `</a>`
       - Snippets: content in `<a class="result__snippet"...>` tags
       - Strip HTML tags from extracted text: remove `<[^>]+>` patterns
       - Limit to first 8 results
   - Save to research cache as ResearchCache JSON
   - Return JSON: `results` array of {source, url, title, snippet}, `from_cache: false`, `result_count: N`
   - On fetch failure: return error JSON (never throw)
   - Note: Per Pitfall 5, write cache file AFTER all results are collected (single write, not concurrent)

5. **fetch_page ToolDefinition:**
   - name: "fetch_page"
   - description: "Fetch a specific URL and extract readable text content. Use after search_web to read a promising result page. Returns up to 8000 characters of cleaned text. Good for WineHQ AppDB pages, PCGamingWiki articles, and forum threads."
   - Required param: `url` (string)

6. **fetch_page implementation** (`private func fetchPage(input: JSONValue) -> String`):
   - Extract and validate URL string
   - Fetch using DispatchSemaphore + ResultBox pattern
   - Set User-Agent header
   - Set timeout to 15 seconds via URLRequest.timeoutInterval
   - Strip HTML tags: remove `<script[^>]*>[\s\S]*?</script>`, `<style[^>]*>[\s\S]*?</style>`, then `<[^>]+>`
   - Decode basic HTML entities: `&amp;` -> `&`, `&lt;` -> `<`, `&gt;` -> `>`, `&quot;` -> `"`, `&#39;` -> `'`
   - Collapse multiple whitespace/newlines into single spaces
   - Truncate to 8000 characters
   - Return JSON: `url`, `content` (cleaned text), `length` (character count), `truncated` (bool)
  </action>
  <verify>
    <automated>cd /Users/peter/Documents/Cellar && swift build 2>&1 | tail -5</automated>
  </verify>
  <done>search_web searches DuckDuckGo for Wine compatibility info with 7-day per-game cache. fetch_page extracts readable text from URLs with HTML stripping. Both use DispatchSemaphore+ResultBox pattern. Registered in execute() and toolDefinitions. Compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Enhanced launch_game + v2 system prompt</name>
  <files>Sources/cellar/Core/AgentTools.swift, Sources/cellar/Core/AIService.swift</files>
  <action>
1. **Enhanced launch_game** in AgentTools.swift:
   - Update `launch_game` ToolDefinition description to mention pre-flight checks and structured DLL analysis
   - Add optional `diagnostic` param (boolean, default false): "If true, this is a quick diagnostic launch (shorter timeout, more verbose output). If false, this is a real launch attempt that counts toward the limit."
   - In launchGame() implementation, add pre-flight checks before launching:
     a. Verify executable exists at executablePath
     b. If accumulatedEnv has WINEDLLOVERRIDES, check that referenced DLL files exist where expected
     c. Return pre-flight warnings in the result JSON (don't block launch, just inform)
   - After launch completes, parse stderr for +loaddll lines (same regex as trace_launch) and include `loaded_dlls` summary in result JSON alongside existing exit_code, elapsed_seconds, stderr_tail, errors
   - Keep existing launch count limit (8) — diagnostic launches (diagnostic=true) do NOT count toward limit

2. **Update system prompt** in AIService.swift `runAgentLoop()`:
   Replace the existing systemPrompt string with the v2 prompt:

   ```
   You are a Wine compatibility expert for macOS. Your job is to get a Windows game running via Wine on macOS.

   ## Three-Phase Workflow: Research -> Diagnose -> Adapt

   You can move between phases non-linearly based on evidence.

   ### Phase 1: Research (before first launch)
   1. Call query_successdb to check for known-working configs for this game or similar games
   2. Call inspect_game to understand the game: exe type, PE imports, bottle type, data files, existing config
   3. If no success record found, call search_web to find Wine compatibility info
   4. If search_web returns promising URLs, call fetch_page to read them
   5. Synthesize research into an initial configuration plan

   ### Phase 2: Diagnose (before configuring)
   1. Call trace_launch to see which DLLs Wine actually loads — this is cheap (3-5 seconds)
   2. Call check_file_access if the game uses relative paths or data files
   3. After placing DLLs or setting overrides, call verify_dll_override to confirm they took effect
   4. Budget: up to 3 diagnostic traces before first real launch, 2 between failed real launches

   ### Phase 3: Adapt (configure and launch)
   1. Based on research and diagnosis, configure environment (set_environment), registry (set_registry), DLLs (place_dll), config files (write_game_file)
   2. Call launch_game for a real launch attempt
   3. After launch, call ask_user to check if the game worked
   4. If yes: call save_success with full details including pitfalls and resolution narrative
   5. If no: read_log, diagnose, loop back to Phase 2 for more investigation

   ## macOS + Wine Domain Knowledge
   - NEVER suggest virtual desktop mode (winemac.drv does not support it on macOS)
   - wow64 bottles have drive_c/windows/syswow64 — 32-bit system DLLs (like ddraw.dll from cnc-ddraw) must go in syswow64, NOT system32
   - cnc-ddraw REQUIRES ddraw.ini with renderer=opengl on macOS (macOS has no D3D9)
   - The game's working directory MUST be the EXE's parent directory (many games use relative paths)
   - PE imports (from inspect_game) show the game's actual DLL dependencies — use this to plan configuration
   - DLL override modes: n=native, b=builtin, n,b=prefer native fall back to builtin
   - WINE_CPU_TOPOLOGY=1:0 helps old single-threaded games
   - WINEDEBUG=-all suppresses debug noise for performance
   - If a game exits immediately (< 2 seconds), it likely has a missing dependency or configuration issue
   - Diagnostic methodology: ALWAYS trace before configuring, verify after placing DLLs

   ## Available Tools (18 total)
   Research: query_successdb, search_web, fetch_page
   Diagnostic: inspect_game, trace_launch, verify_dll_override, check_file_access, read_log, read_registry
   Action: set_environment, set_registry, install_winetricks, place_dll, write_game_file, launch_game
   User: ask_user
   Persistence: save_success, save_recipe

   ## Constraints
   - Maximum 8 real launch attempts — be strategic, use diagnostics first
   - Diagnostic launches (trace_launch) are free — use them liberally
   - Only install winetricks verbs from the allowed list
   - Only place DLLs from the known DLL registry
   - All operations are sandboxed to the game's bottle and ~/.cellar/

   ## Communication
   - Explain your reasoning as you go — what you found in research, what the trace revealed, why you're trying a specific fix
   - If you exhaust attempts, write a detailed summary including pitfalls discovered
   ```

   Also update the initial message to reference the three-phase workflow:
   ```swift
   let initialMessage = "Launch the game '\(entry.name)' (ID: \(gameId)). The executable is at: \(executablePath). Follow the Research-Diagnose-Adapt workflow: start by querying the success database, then inspect the game."
   ```
  </action>
  <verify>
    <automated>cd /Users/peter/Documents/Cellar && swift build 2>&1 | tail -5</automated>
  </verify>
  <done>launch_game has pre-flight checks, structured DLL analysis in output, and diagnostic mode. System prompt implements Research-Diagnose-Adapt three-phase workflow with correct macOS/Wine domain knowledge, references all 18 tools, removes virtual desktop suggestion. Compiles.</done>
</task>

</tasks>

<verification>
- `swift build` succeeds
- AgentTools has search_web and fetch_page in execute() switch and toolDefinitions
- AIService system prompt contains "Research -> Diagnose -> Adapt" and "NEVER suggest virtual desktop"
- System prompt references all new tools: trace_launch, verify_dll_override, check_file_access, search_web, fetch_page, write_game_file, query_successdb, save_success
- launch_game includes pre-flight checks and loaded_dlls in result
- Research cache uses CellarPaths.researchCacheFile
</verification>

<success_criteria>
The v2 agent architecture is complete: 18 tools (10 original + 8 new), updated system prompt with Research-Diagnose-Adapt workflow, correct macOS/Wine domain knowledge, research caching with 7-day TTL, and enhanced launch_game with diagnostic output. The agent can now research before configuring, diagnose before acting, and save comprehensive success records.
</success_criteria>

<output>
After completion, create `.planning/phases/07-agentic-v2-research-diagnose-adapt-loop-with-web-search-diagnostic-traces-and-success-database/07-05-SUMMARY.md`
</output>
