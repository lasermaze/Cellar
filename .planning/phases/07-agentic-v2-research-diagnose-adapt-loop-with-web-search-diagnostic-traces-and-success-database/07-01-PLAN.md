---
phase: 07-agentic-v2-research-diagnose-adapt-loop-with-web-search-diagnostic-traces-and-success-database
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/cellar/Core/WineProcess.swift
  - Sources/cellar/Core/WineErrorParser.swift
  - Sources/cellar/Models/KnownDLLRegistry.swift
  - Sources/cellar/Persistence/CellarPaths.swift
autonomous: true
requirements: []

must_haves:
  truths:
    - "WineProcess.run() sets process CWD to the game binary's parent directory"
    - "DLLPlacementTarget has .syswow64 case with autoDetect logic"
    - "KnownDLL struct has companionFiles, preferredTarget, and isSystemDLL fields"
    - "CellarPaths provides successdbDir, researchCacheDir, and helper methods"
  artifacts:
    - path: "Sources/cellar/Core/WineProcess.swift"
      provides: "CWD fix in run() method"
      contains: "currentDirectoryURL = binaryURL.deletingLastPathComponent()"
    - path: "Sources/cellar/Core/WineErrorParser.swift"
      provides: "DLLPlacementTarget.syswow64 case + autoDetect static method"
      contains: "case syswow64"
    - path: "Sources/cellar/Models/KnownDLLRegistry.swift"
      provides: "CompanionFile struct, extended KnownDLL with companionFiles/preferredTarget/isSystemDLL"
      contains: "struct CompanionFile"
    - path: "Sources/cellar/Persistence/CellarPaths.swift"
      provides: "successdbDir, researchCacheDir paths"
      contains: "successdbDir"
  key_links:
    - from: "Sources/cellar/Core/WineErrorParser.swift"
      to: "Sources/cellar/Models/KnownDLLRegistry.swift"
      via: "DLLPlacementTarget used in KnownDLL.preferredTarget"
      pattern: "preferredTarget.*DLLPlacementTarget"
---

<objective>
P0 infrastructure fixes and model extensions that unblock all subsequent Phase 7 work.

Purpose: Fix the WineProcess CWD bug (games using relative paths fail), add syswow64 DLL placement support (wow64 bottles need 32-bit system DLLs in syswow64), extend KnownDLLRegistry with companion file metadata (cnc-ddraw needs ddraw.ini), and add CellarPaths for success DB and research cache directories.
Output: Fixed WineProcess, extended DLLPlacementTarget/KnownDLL/CellarPaths — all prerequisites for plans 02-05.
</objective>

<execution_context>
@/Users/peter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Sources/cellar/Core/WineProcess.swift
@Sources/cellar/Core/WineErrorParser.swift
@Sources/cellar/Models/KnownDLLRegistry.swift
@Sources/cellar/Persistence/CellarPaths.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: WineProcess CWD fix + DLLPlacementTarget.syswow64 + KnownDLL extensions</name>
  <files>Sources/cellar/Core/WineProcess.swift, Sources/cellar/Core/WineErrorParser.swift, Sources/cellar/Models/KnownDLLRegistry.swift</files>
  <action>
1. In WineProcess.swift `run()` method: After setting `process.executableURL` and `process.arguments`, add CWD setting:
   ```swift
   let binaryURL = URL(fileURLWithPath: binary)
   process.currentDirectoryURL = binaryURL.deletingLastPathComponent()
   ```
   This fixes games that use relative paths (e.g., Missions/Missions.txt, mode.dat).

2. In WineErrorParser.swift, extend `DLLPlacementTarget` enum:
   - Add `case syswow64` after `case system32` with comment: "Wine's SysWOW64 -- 32-bit system DLLs in wow64 bottles"
   - Add static method `autoDetect(bottleURL: URL, dllBitness: Int, isSystemDLL: Bool) -> DLLPlacementTarget`:
     - Check if `drive_c/windows/syswow64` directory exists in bottleURL
     - If isSystemDLL AND isWow64 AND dllBitness == 32: return .syswow64
     - Otherwise return .gameDir

3. In KnownDLLRegistry.swift:
   - Add `struct CompanionFile` with `let filename: String` and `let content: String`
   - Extend `KnownDLL` struct with three new fields:
     - `let companionFiles: [CompanionFile]` (default empty in existing entries)
     - `let preferredTarget: DLLPlacementTarget` (default .gameDir)
     - `let isSystemDLL: Bool` (default false)
   - Update the cnc-ddraw registry entry:
     - `companionFiles: [CompanionFile(filename: "ddraw.ini", content: "[ddraw]\nrenderer=opengl\nfullscreen=true\nhandlemouse=true\nadjmouse=true\ndevmode=0\nmaxgameticks=0\nnonexclusive=false\nsinglecpu=true")]`
     - `preferredTarget: .syswow64`
     - `isSystemDLL: true`
  </action>
  <verify>
    <automated>cd /Users/peter/Documents/Cellar && swift build 2>&1 | tail -5</automated>
  </verify>
  <done>WineProcess.run() sets CWD to binary parent dir. DLLPlacementTarget has .syswow64 with autoDetect(). KnownDLL has companionFiles/preferredTarget/isSystemDLL. cnc-ddraw entry updated with ddraw.ini companion and syswow64 target. Project compiles.</done>
</task>

<task type="auto">
  <name>Task 2: CellarPaths extensions for success DB and research cache</name>
  <files>Sources/cellar/Persistence/CellarPaths.swift</files>
  <action>
Add to CellarPaths struct:

```swift
// Success database directory: ~/.cellar/successdb/
static let successdbDir: URL = base.appendingPathComponent("successdb")

static func successdbFile(for gameId: String) -> URL {
    successdbDir.appendingPathComponent("\(gameId).json")
}

// Research cache directory: ~/.cellar/research/
static let researchCacheDir: URL = base.appendingPathComponent("research")

static func researchCacheFile(for gameId: String) -> URL {
    researchCacheDir.appendingPathComponent("\(gameId).json")
}
```

These follow the exact same pattern as existing `dllsDir`, `bottlesDir`, etc.
  </action>
  <verify>
    <automated>cd /Users/peter/Documents/Cellar && swift build 2>&1 | tail -5</automated>
  </verify>
  <done>CellarPaths has successdbDir, successdbFile(for:), researchCacheDir, researchCacheFile(for:) static members. Project compiles.</done>
</task>

</tasks>

<verification>
- `swift build` succeeds with no errors
- WineProcess.swift contains `currentDirectoryURL` assignment
- WineErrorParser.swift contains `case syswow64` and `autoDetect` method
- KnownDLLRegistry.swift contains `CompanionFile` struct and updated cnc-ddraw entry
- CellarPaths.swift contains `successdbDir` and `researchCacheDir`
</verification>

<success_criteria>
All four files compile. The WineProcess CWD fix is in place. DLLPlacementTarget.syswow64 exists with autoDetect logic. KnownDLL has companion file metadata. CellarPaths provides success DB and research cache paths.
</success_criteria>

<output>
After completion, create `.planning/phases/07-agentic-v2-research-diagnose-adapt-loop-with-web-search-diagnostic-traces-and-success-database/07-01-SUMMARY.md`
</output>
