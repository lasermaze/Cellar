---
phase: 09-engine-detection-and-pre-configuration
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - Sources/cellar/Core/AIService.swift
autonomous: true
requirements:
  - ENGN-03
  - ENGN-04

must_haves:
  truths:
    - "System prompt tells the agent to check engine and graphics_api fields after inspect_game and pre-configure known engines before first launch"
    - "System prompt provides engine-specific pre-configuration guidance for DirectDraw games (cnc-ddraw + ddraw.ini), OpenGL games (MESA overrides), and modern engines (fewer tweaks)"
    - "System prompt tells the agent to include engine name and graphics API in search_web queries instead of game-name-only queries"
    - "System prompt tells the agent to query_successdb with engine and graphics_api params after engine detection for cross-game solutions"
    - "Agent uses existing tools (set_registry, write_game_file, set_environment, place_dll) for pre-configuration — no new tools created"
  artifacts:
    - path: "Sources/cellar/Core/AIService.swift"
      provides: "Engine-aware system prompt with pre-configuration methodology, search enrichment guidance, and success database cross-referencing"
      contains: "Engine-Aware Methodology"
  key_links:
    - from: "Sources/cellar/Core/AIService.swift"
      to: "Sources/cellar/Core/AgentTools.swift"
      via: "System prompt references engine/graphics_api fields returned by inspect_game tool"
      pattern: "engine.*graphics_api"
    - from: "Sources/cellar/Core/AIService.swift"
      to: "Sources/cellar/Core/AgentTools.swift"
      via: "System prompt references query_successdb engine/graphics_api params and search_web queries"
      pattern: "query_successdb.*engine"
---

<objective>
Update the agent system prompt with engine-aware methodology: pre-configuration guidance for known engines, search query enrichment with engine and graphics API, and success database cross-referencing by engine type.

Purpose: ENGN-03 requires pre-configuration before first launch for known engines. ENGN-04 requires engine-aware search queries. Both are prompt-level guidance using existing tools — no new code, just smarter agent behavior.
Output: Updated system prompt in AIService.swift.
</objective>

<execution_context>
@/Users/peter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-engine-detection-and-pre-configuration/09-RESEARCH.md
@.planning/phases/09-engine-detection-and-pre-configuration/09-01-SUMMARY.md

<interfaces>
<!-- Key interfaces from the codebase that this plan references -->

From Sources/cellar/Core/AIService.swift (lines 510-566):
- System prompt is a multi-line string literal starting at line 510
- Contains sections: Three-Phase Workflow (Research/Diagnose/Adapt), macOS + Wine Domain Knowledge, Available Tools, Constraints, Communication
- Prompt is passed to AgentLoop constructor at line 582

From Sources/cellar/Core/AgentTools.swift (after Plan 01):
- inspect_game result now includes: engine (String), engine_confidence (String), engine_family (String), detected_signals ([String]), graphics_api (String) — when detection succeeds
- query_successdb accepts engine and graphics_api query params (already exists)
- search_web accepts free-text query (already exists)
- write_game_file, set_registry, set_environment, place_dll — all exist for pre-configuration

From Sources/cellar/Models/EngineRegistry.swift (created by Plan 01):
- 8 engine families: GSC/DMCR, Unreal 1, Build, id Tech 2/3, Unity, UE4/5, Westwood, Blizzard
- Family IDs: gsc, unreal1, build, idtech, unity, unreal4, westwood, blizzard
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add engine-aware methodology section to system prompt</name>
  <files>Sources/cellar/Core/AIService.swift</files>
  <action>
In AIService.swift, find the system prompt string starting at line 510 (inside the `runAgentLoop` method). Add a new section `## Engine-Aware Methodology` AFTER the existing `## Three-Phase Workflow` section and BEFORE `## macOS + Wine Domain Knowledge`. This placement ensures the agent reads engine guidance right after learning the workflow phases.

The new section content (add as a string continuation in the multi-line literal):

```
## Engine-Aware Methodology

After calling inspect_game, check the engine and graphics_api fields in the result:

### Pre-Configuration (before first launch)
If engine is detected with medium or high confidence, pre-configure the game BEFORE attempting the first launch:

- **DirectDraw games** (GSC/DMCR, Build, Westwood, Blizzard — graphics_api: directdraw): These games need cnc-ddraw. Call place_dll with name "cnc-ddraw", then verify ddraw.ini exists in the game directory with renderer=opengl (use write_game_file if needed). This skips the renderer selection dialog that blocks these games.
- **id Tech 2/3 games** (graphics_api: opengl): These use OpenGL natively and usually work well under Wine. If you see rendering issues, set MESA_GL_VERSION_OVERRIDE=4.5 via set_environment.
- **Unreal 1 games** (graphics_api: direct3d9 or direct3d8): May need d3d9/d3d8 DLL configuration. Check if the game has a renderer selection INI (like UnrealTournament.ini) and pre-set the renderer to Direct3D 9.
- **Unity games**: Look for screen resolution dialog on first launch. If detected via trace_launch, write a registry key or prefs file to skip it.
- **UE4/5 games**: Modern engine, usually needs fewer Wine tweaks. Check for D3D11 requirements.

Pre-configuration uses existing tools: place_dll, write_game_file, set_registry, set_environment. Do NOT skip pre-configuration for known engines — it prevents wasted launch attempts on renderer dialogs.

### Search Query Enrichment
When searching for solutions, include the detected engine and graphics API in your queries:
- Good: "GSC engine DirectDraw renderer selection dialog Wine macOS"
- Good: "Build engine Duke Nukem 3D cnc-ddraw Wine crashes"
- Bad: "Duke Nukem 3D Wine macOS" (too generic, misses engine-specific solutions)

Always combine: [engine name] + [graphics API] + [specific symptom] + "Wine macOS"

### Success Database Cross-Reference
After engine detection, ALWAYS call query_successdb with the engine family and graphics_api:
- query_successdb(engine: "gsc") finds configs from other GSC games
- query_successdb(graphics_api: "directdraw") finds configs from other DirectDraw games
Cross-game solutions are highly reliable because games on the same engine share the same Wine compatibility patterns.
```

Also update the Phase 1 Research section (line ~518-522) to mention engine detection. After step 2 ("Call inspect_game..."), add:
"2b. Check the engine and graphics_api fields — if an engine is detected, pre-configure known settings before proceeding to launch (see Engine-Aware Methodology above)"

This is a prompt-only change. No new functions, types, or tools.
  </action>
  <verify>
    <automated>cd /Users/peter/Documents/Cellar && swift build 2>&1 | tail -5</automated>
  </verify>
  <done>System prompt contains "Engine-Aware Methodology" section with pre-configuration guidance for DirectDraw/OpenGL/Unreal/Unity/UE4 engines, search query enrichment instructions, and success database cross-referencing guidance. swift build compiles cleanly.</done>
</task>

</tasks>

<verification>
1. `swift build` compiles with no errors
2. System prompt in AIService.swift contains "Engine-Aware Methodology" section
3. Prompt mentions all 8 engine families with appropriate pre-configuration guidance
4. Prompt instructs agent to include engine + graphics_api in search_web queries
5. Prompt instructs agent to call query_successdb with engine and graphics_api after detection
6. No new tools, structs, or functions created — purely prompt-level changes
</verification>

<success_criteria>
- AIService.swift system prompt includes engine-aware pre-configuration guidance
- Agent is instructed to pre-configure DirectDraw games with cnc-ddraw before first launch
- Agent is instructed to include engine name and graphics API in search queries
- Agent is instructed to query success database by engine type for cross-game solutions
- Project compiles cleanly with no new dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/09-engine-detection-and-pre-configuration/09-02-SUMMARY.md`
</output>
