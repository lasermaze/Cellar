---
phase: 27-distribution-github-releases-install-script
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.github/workflows/release.yml]
autonomous: true
requirements: ["Release workflow cleanup (checksum + smoke test)"]

must_haves:
  truths:
    - "Release workflow generates a .sha256 checksum file alongside the archive"
    - "Release workflow runs a smoke test (cellar --help) before uploading"
    - "Release workflow uploads both archive and checksum to GitHub Releases"
    - "Release workflow generates release notes automatically"
    - "Homebrew formula update step is removed from the workflow"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "CI release pipeline with checksum and smoke test"
      contains: "shasum -a 256"
  key_links:
    - from: ".github/workflows/release.yml"
      to: "GitHub Releases"
      via: "softprops/action-gh-release with files + generate_release_notes"
      pattern: "generate_release_notes.*true"
---

<objective>
Clean up the existing release CI workflow: remove the Homebrew formula update step, add a smoke test before upload, generate a SHA-256 checksum file, upload both archive and checksum to GitHub Releases, and enable automatic release notes.

Purpose: The release workflow currently has a Homebrew formula update step that pushes to main and is no longer needed. Adding checksum generation enables the install.sh script (Plan 02) to verify downloads. The smoke test catches broken builds before they reach users.

Output: Modified `.github/workflows/release.yml`
</objective>

<execution_context>
@/Users/peter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/27-distribution-github-releases-install-script/27-CONTEXT.md
@.planning/phases/27-distribution-github-releases-install-script/27-RESEARCH.md

<interfaces>
<!-- Current release.yml structure (46 lines):
  - Trigger: push tags v*
  - Job: release on macos-latest, contents: write
  - Step 1: actions/checkout@v4
  - Step 2: Build universal binary (swift build, tar, set ARCHIVE/VERSION env vars)
  - Step 3: Upload to GitHub Releases (softprops/action-gh-release@v2)
  - Step 4: Update Homebrew formula (sed + git push — TO BE REMOVED)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Homebrew step, add smoke test and checksum, enable release notes</name>
  <files>.github/workflows/release.yml</files>
  <action>
Modify `.github/workflows/release.yml` with these exact changes:

1. **Remove the "Update Homebrew formula" step entirely** (lines 31-46 in current file — the entire last step with sed, git config, git add, git commit, git push).

2. **Add a "Smoke test" step** immediately after the "Build universal binary" step:
```yaml
    - name: Smoke test
      run: .build/apple/Products/Release/cellar --help
```

3. **Add a "Generate checksum" step** after the smoke test:
```yaml
    - name: Generate checksum
      run: shasum -a 256 "$ARCHIVE" > "${ARCHIVE}.sha256"
```

4. **Update the "Upload to GitHub Releases" step** to:
   - Upload both the archive and the .sha256 file using YAML multiline literal block for `files:`
   - Add `generate_release_notes: true`

The final upload step should look like:
```yaml
    - name: Upload to GitHub Releases
      uses: softprops/action-gh-release@v2
      with:
        files: |
          ${{ env.ARCHIVE }}
          ${{ env.ARCHIVE }}.sha256
        generate_release_notes: true
        token: ${{ secrets.GITHUB_TOKEN }}
```

The resulting workflow should have exactly 5 steps: checkout, build, smoke test, checksum, upload. No Homebrew step.
  </action>
  <verify>
    <automated>grep -c "shasum" .github/workflows/release.yml | grep -q "1" && grep -c "Homebrew" .github/workflows/release.yml | grep -q "0" && grep -c "generate_release_notes" .github/workflows/release.yml | grep -q "1" && grep -c "Smoke test" .github/workflows/release.yml | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>release.yml has 5 steps (checkout, build, smoke test, checksum, upload), no Homebrew step, generates .sha256 file, uploads both files, and enables auto release notes</done>
</task>

</tasks>

<verification>
- `grep "Homebrew" .github/workflows/release.yml` returns nothing (step removed)
- `grep "shasum" .github/workflows/release.yml` finds the checksum generation line
- `grep "generate_release_notes" .github/workflows/release.yml` finds `true`
- `grep "Smoke test" .github/workflows/release.yml` finds the smoke test step
- `grep "\.sha256" .github/workflows/release.yml` finds the checksum file in both generation and upload
</verification>

<success_criteria>
The release workflow builds the universal binary, runs a smoke test, generates a SHA-256 checksum file, uploads both archive and checksum to GitHub Releases with auto-generated release notes, and does NOT contain any Homebrew formula update logic.
</success_criteria>

<output>
After completion, create `.planning/phases/27-distribution-github-releases-install-script/27-01-SUMMARY.md`
</output>
