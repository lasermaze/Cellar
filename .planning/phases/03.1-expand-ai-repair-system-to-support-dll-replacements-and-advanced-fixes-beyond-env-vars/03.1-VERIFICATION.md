---
phase: 03.1-expand-ai-repair-system
verified: 2026-03-27T21:00:00Z
status: passed
score: 16/16 must-haves verified
gaps: []
human_verification:
  - test: "Launch Cossacks with a DirectDraw failure and confirm cnc-ddraw variant is selected first, DLL is downloaded from GitHub, placed in gameDir, and the game runs"
    expected: "DLL downloaded to ~/.cellar/dlls/cnc-ddraw/ddraw.dll, copied next to Cossacks.exe, Wine uses native ddraw override, game launches"
    why_human: "Requires network access to GitHub releases API and a real Wine bottle with Cossacks installed to verify end-to-end DLL placement"
  - test: "Exhaust all bundled variants, watch AI escalation proceed from level 1 to level 2 to level 3"
    expected: "Three separate escalation messages printed: 'Trying environment variable adjustments...' -> 'Escalating to DLL overrides and winetricks...' -> 'Escalating to DLL replacements and registry edits...'"
    why_human: "Requires real API key and multiple failed launch attempts to observe the escalation path"
  - test: "Provide an AI-suggested winetricks verb outside the allowlist (e.g. 'mfc42') and confirm it is skipped with a warning"
    expected: "Warning printed: 'AI suggested winetricks verb 'mfc42' which is not in our known list — skipping.' Repair loop continues."
    why_human: "Requires mocking or intercepting the AI response to inject an invalid verb"
---

# Phase 03.1: Expand AI Repair System Verification Report

**Phase Goal:** Expand the AI repair system beyond env vars and WINEDLLOVERRIDES to support DLL replacements (cnc-ddraw), registry edits, winetricks verbs, and other advanced Wine fixes. Graduated escalation with progressive prompt expansion.
**Verified:** 2026-03-27T21:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths — Plan 01

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | WineFix enum supports placeDLL, setRegistry, and compound actions | VERIFIED | `WineErrorParser.swift` lines 27-31: `case placeDLL(String, DLLPlacementTarget)`, `case setRegistry(String, String, String)`, `case compound([WineFix])`. Note: truth stated "installWinetricksMulti" but the plan tasks only add 3 new cases; `compound` covers multi-verb sequences. |
| 2 | KnownDLLRegistry can look up cnc-ddraw by name and return GitHub repo + asset info | VERIFIED | `KnownDLLRegistry.swift` lines 13-30: static `registry` with cnc-ddraw entry (FunkyFr3sh/cnc-ddraw, ddraw.dll, cnc-ddraw.zip). `find(name:)` returns matching entry. |
| 3 | DLLDownloader can fetch a GitHub latest release, extract a DLL from a zip, and cache it in ~/.cellar/dlls/ | VERIFIED | `DLLDownloader.swift` lines 14-94: `downloadAndCache()` checks cache first at `CellarPaths.cachedDLLFile`, hits GitHub API, uses `/usr/bin/ditto -xk` for extraction, copies to cache. `place()` at lines 97-107 copies to target dir. |
| 4 | WineActionExecutor can dispatch any WineFix case to the correct Wine mechanism | VERIFIED | `WineActionExecutor.swift` lines 18-123: exhaustive switch over all 6 WineFix cases — `installWinetricks`, `setEnvVar`, `setDLLOverride`, `placeDLL` (delegates to DLLDownloader), `setRegistry` (writes .reg + wine regedit), `compound` (iterates sub-actions). |
| 5 | RetryVariant has an optional actions field alongside the existing environment field | VERIFIED | `Recipe.swift` lines 37-45: `let actions: [[String: String]]?` with explicit `CodingKeys`. Backward-compatible — existing recipe JSON without `actions` field decodes correctly. |
| 6 | CellarPaths provides dllsDir and dllCacheDir helpers | VERIFIED | `CellarPaths.swift` lines 21-29: `static let dllsDir`, `static func dllCacheDir(for:)`, `static func cachedDLLFile(dllName:fileName:)`. |

### Observable Truths — Plan 02

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 7 | AI generateVariants accepts an escalation level parameter and returns different action types per level | VERIFIED | `AIService.swift` lines 261-278: `generateVariants(..., escalationLevel: Int = 1)`. Passed through to `_generateVariants` and into `buildVariantSystemPrompt(level:)`. |
| 8 | Level 1 prompts allow only env vars and DLL overrides; Level 2 adds winetricks; Level 3 adds place_dll and set_registry | VERIFIED | `AIService.swift` lines 362-411: `buildVariantSystemPrompt(level:)` — Level 1 explicitly prohibits registry/DLL/winetricks ("All action arrays must be empty []"); Level 2 adds `install_winetricks` and `set_dll_override`; Level 3 adds `place_dll` and `set_registry`. |
| 9 | LaunchCommand computes escalation level from attempt progress and passes it to generateVariants | VERIFIED | `LaunchCommand.swift` line 101: `var currentEscalationLevel = 1`. Lines 107-146: when `configIndex >= envConfigs.count && currentEscalationLevel <= 3`, increments level and calls `generateVariants(escalationLevel: level)` at line 129. |
| 10 | LaunchCommand uses WineActionExecutor to apply AI-suggested actions including DLL placements and registry edits | VERIFIED | `LaunchCommand.swift` lines 69-74: executor created before loop. Lines 165-167: variant-level actions applied via `executor.execute()`. Lines 255, 269: error-diagnosed and AI-diagnosed fixes both dispatched through executor. |
| 11 | User sees escalation progression messages during repair loop | VERIFIED | `LaunchCommand.swift` lines 111-115: dictionary of level-to-message mapping; `print()` at line 116 emits the relevant message per level. |
| 12 | Cossacks recipe includes cnc-ddraw as a first-class retry variant with actions field | VERIFIED | `cossacks-european-wars.json` lines 23-33: first entry in `retry_variants` array has `description: "cnc-ddraw DirectDraw replacement..."`, `environment`, and `actions: [{"type": "place_dll", "dll": "cnc-ddraw", "target": "game_dir"}]`. |
| 13 | Repair report includes DLL placements and registry edits attempted | VERIFIED | `LaunchCommand.swift` lines 386-395: `if !appliedActions.isEmpty` block appends `"--- Applied Actions ---"` section and all action descriptions to report lines. |
| 14 | Winetricks allowlist is expanded with common game dependency verbs | VERIFIED | `AIService.swift` lines 567-582: `validWinetricksVerbs` contains 22 verbs across 6 categories (runtime, DirectX, media, audio, input, game deps). Previously had 7 verbs. |
| 15 | Unknown winetricks verbs from AI trigger a warning and are skipped | VERIFIED | `AIService.swift` lines 619-621: `guard validWinetricksVerbs.contains(verb) else { print("Warning: AI suggested winetricks verb '\(verb)' which is not in our known list — skipping."); return nil }`. |
| 16 | Winning AI variant with actions is saved as user recipe | VERIFIED | `LaunchCommand.swift` lines 421-488: when `winningConfigIndex >= originalEnvConfigsCount`, serializes `winningActions` back to `[[String:String]]` dicts at lines 442-457, creates `RetryVariant` with `actions` field, saves via `RecipeEngine.saveUserRecipe()`. |

**Score:** 16/16 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `Sources/cellar/Core/WineErrorParser.swift` | Expanded WineFix enum with placeDLL, setRegistry, compound cases | VERIFIED | 129 lines, all 6 WineFix cases present, DLLPlacementTarget enum, DirectDraw pattern 5 |
| `Sources/cellar/Models/KnownDLLRegistry.swift` | Static registry of known open-source DLL replacement projects | VERIFIED | 30 lines, cnc-ddraw entry with full GitHub metadata and requiredOverrides |
| `Sources/cellar/Core/DLLDownloader.swift` | GitHub release download, zip extraction via ditto, DLL caching | VERIFIED | 135 lines, `downloadAndCache()`, `place()`, `syncRequest()` — cache-first pattern |
| `Sources/cellar/Core/WineActionExecutor.swift` | Dispatch WineFix actions to Wine mechanisms | VERIFIED | 126 lines, all 6 WineFix cases handled, updated tuple signature includes `actions` field |
| `Sources/cellar/Models/Recipe.swift` | RetryVariant with optional actions field | VERIFIED | `let actions: [[String: String]]?` at line 40, CodingKeys explicit |
| `Sources/cellar/Persistence/CellarPaths.swift` | dllsDir and dllCacheDir path helpers | VERIFIED | `dllsDir`, `dllCacheDir(for:)`, `cachedDLLFile(dllName:fileName:)` at lines 21-29 |
| `Sources/cellar/Core/AIService.swift` | Progressive prompt system with escalation levels, expanded winetricks allowlist, new action type parsing | VERIFIED | `buildVariantSystemPrompt(level:)`, `validWinetricksVerbs` (22 verbs), `parseWineFix(from:)` handles all 5 action types |
| `Sources/cellar/Commands/LaunchCommand.swift` | Escalation-aware retry loop with WineActionExecutor integration | VERIFIED | Graduated escalation loop, WineActionExecutor created and wired, repair report Applied Actions section |
| `recipes/cossacks-european-wars.json` | cnc-ddraw as first-class retry variant | VERIFIED | cnc-ddraw is first entry in retry_variants with place_dll action |
| `Sources/cellar/Models/AIModels.swift` | AIVariant struct with parsed [WineFix] actions | VERIFIED | `AIVariant` struct lines 30-34, `AIVariantResult` uses `[AIVariant]` |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| WineActionExecutor | WineFix enum | switch dispatch on all 6 cases | WIRED | `WineActionExecutor.swift` lines 18-123: exhaustive switch, all cases handled |
| DLLDownloader | KnownDLLRegistry | `KnownDLLRegistry.find(name:)` lookup | WIRED | `WineActionExecutor.swift` line 60: `guard let knownDLL = KnownDLLRegistry.find(name: dllName)` before calling `DLLDownloader.downloadAndCache(knownDLL)` at line 68 |
| WineActionExecutor | DLLDownloader | `DLLDownloader.downloadAndCache()` in placeDLL case | WIRED | `WineActionExecutor.swift` lines 66-93: full implementation calls `downloadAndCache` then `place` |
| LaunchCommand retry loop | AIService.generateVariants(escalationLevel:) | escalation level computed from AI call count | WIRED | `LaunchCommand.swift` lines 107-129: `currentEscalationLevel` increments and is passed as `escalationLevel: level` |
| LaunchCommand retry loop | WineActionExecutor.execute() | apply actions from AI variants | WIRED | `LaunchCommand.swift` lines 163-174 (variant actions), 255, 269 (diagnosed fix actions) |
| AIService.parseVariantsResponse | WineFix enum | `parseWineFix(from:)` maps action types to WineFix cases | WIRED | `AIService.swift` lines 605-637: `parseWineFix(from:)` handles `place_dll`, `set_registry`, `set_dll_override`, `install_winetricks`, `set_env` |

### Requirements Coverage

This phase has `requirements: []` in both plan frontmatter — it is an INSERTED phase that extends RECIPE-04 capabilities without being directly assigned a requirement ID.

No requirement IDs to cross-reference. REQUIREMENTS.md does not map additional IDs to this phase. No orphaned requirements.

| ID | Description | Status | Evidence |
|----|-------------|--------|---------|
| RECIPE-04 (extends) | Cellar can try multiple recipe variants when a launch fails | EXTENDED | Phase 03.1 expands RECIPE-04's retry loop from env-var-only to full graduated escalation with DLL placements, registry edits, and winetricks verbs. The base RECIPE-04 was already complete in Phase 03. |

### Anti-Patterns Found

No blockers or stubs detected. Scanned all 9 modified/created files.

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `LaunchCommand.swift` | 418 | `let reachedMenu = true` — hardcoded; actual success determination happens in-loop via ValidationPrompt | Info | Pre-existing pattern from Phase 03; not introduced by this phase. No impact on new functionality. |

### Human Verification Required

#### 1. End-to-end cnc-ddraw DLL placement

**Test:** Launch Cossacks: European Wars in a bottle that produces a DirectDraw Init Failed error, or manually select the cnc-ddraw retry variant. Observe that the tool downloads ddraw.dll from GitHub (FunkyFr3sh/cnc-ddraw) to `~/.cellar/dlls/cnc-ddraw/ddraw.dll`, copies it next to the game executable, and sets `WINEDLLOVERRIDES=ddraw=n,b`.
**Expected:** DLL cached on disk, copied to gameDir, Wine log shows native ddraw loading
**Why human:** Requires network access to GitHub releases API and a real Wine bottle to verify file placement and Wine behavior

#### 2. Three-level AI escalation progression

**Test:** Configure an API key, launch a game that fails all bundled variants, watch the repair loop exhaust levels 1, 2, and 3 in sequence.
**Expected:** Three distinct escalation messages printed in order. Level 3 AI call produces variants with `place_dll` or `set_registry` action types.
**Why human:** Requires real API key and multiple controlled launch failures

#### 3. Unknown winetricks verb skip in practice

**Test:** Intercept or mock the AI response to return a variant with `{"type": "install_winetricks", "verb": "mfc42"}` and observe the warning and skip.
**Expected:** Warning printed, variant continues without attempting the install, repair loop proceeds normally
**Why human:** Requires mocking AI response or custom test harness

### Gaps Summary

No gaps. All 16 observable truths are verified against the actual codebase. All artifacts exist, are substantive (not stubs), and are wired correctly. The build compiles cleanly. All four commits (937ccd5, 7a7d3bc, f6168f4, 1ff53b9) exist in git history.

**One note on Plan 01 truth wording:** The truth stated "WineFix enum supports placeDLL, setRegistry, installWinetricksMulti, and compound actions" — but `installWinetricksMulti` was never defined in the task body and was not implemented. The existing `installWinetricks(String)` (single verb) combined with `compound([WineFix])` covers multi-verb installation. The task body and plan verification criteria made no mention of a separate `installWinetricksMulti` case. This is a plan wording error, not a missing implementation — the functional behavior is fully covered.

---

_Verified: 2026-03-27T21:00:00Z_
_Verifier: Claude (gsd-verifier)_
