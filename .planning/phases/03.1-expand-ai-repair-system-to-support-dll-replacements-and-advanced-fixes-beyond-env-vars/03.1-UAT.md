---
status: complete
phase: 03.1-expand-ai-repair-system
source: [03.1-01-SUMMARY.md, 03.1-02-SUMMARY.md]
started: 2026-03-27T21:00:00Z
updated: 2026-03-27T21:05:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cossacks launches with cnc-ddraw retry variant
expected: Run `swift run cellar launch cossacks-european-wars` (with the game added and ANTHROPIC_API_KEY or OPENAI_API_KEY set). The first attempt may fail with a DirectDraw error. Cellar should then try the cnc-ddraw retry variant — you should see it download cnc-ddraw from GitHub, place it in the game directory, and retry with WINEDLLOVERRIDES=ddraw=n,b. Look for output like "Downloading cnc-ddraw..." and "Placing DLL in game directory...".
result: skipped
reason: Requires runtime Wine + game files — manual testing

### 2. Graduated escalation progression messages
expected: During a multi-attempt launch where variants keep failing, you should see escalation level messages as Cellar tries progressively more powerful fixes. Look for messages indicating Level 1 (env var tweaks), then Level 2 (adding DLL overrides + winetricks), then Level 3 (DLL replacements + registry edits). The repair loop should not stay stuck at one level.
result: skipped
reason: Requires runtime Wine + API key + multiple failures — manual testing

### 3. AI suggests advanced fix types at higher escalation levels
expected: At escalation Level 2+, the AI should be able to suggest winetricks verb installs and DLL overrides. At Level 3, it should be able to suggest DLL replacements (place_dll) and registry edits (set_registry). You can verify by checking the AI analysis output — at Level 1 it should only suggest env vars, at higher levels it should suggest richer fixes.
result: skipped
reason: Requires runtime AI API calls — manual testing

### 4. DLL download caching
expected: If cnc-ddraw was downloaded once (check ~/.cellar/dlls/cnc-ddraw/), a second launch should reuse the cached copy without re-downloading. You should NOT see "Downloading cnc-ddraw..." on subsequent launches — it should go straight to placing the DLL.
result: skipped
reason: Requires runtime DLL download — manual testing

### 5. Winning AI variant saved as recipe
expected: If an AI-suggested variant with advanced actions (DLL placement, registry edit, etc.) succeeds and you confirm the game reached the menu, Cellar should save the winning configuration as a user recipe at ~/.cellar/recipes/. The saved recipe should include the actions that worked (not just env vars).
result: skipped
reason: Requires runtime Wine + successful game launch — manual testing

### 6. Repair report includes all fix types
expected: After all attempts are exhausted (bundled + AI variants at all escalation levels), Cellar writes a repair report. The report should include an "Applied Actions" section showing DLL placements, registry edits, and other advanced fixes that were tried — not just env var changes.
result: skipped
reason: Requires runtime full exhaustion loop — manual testing

### 7. Unknown wiretricks verb warning
expected: If the AI suggests a winetricks verb that isn't in the known allowlist, Cellar should print a warning message about the unknown verb and skip it rather than silently failing or crashing. The repair loop should continue to the next fix.
result: skipped
reason: Requires mocking AI response — manual testing

### 8. Build compiles cleanly
expected: Run `swift build` from the project root. The project should compile without errors. All new types (WineActionExecutor, DLLDownloader, KnownDLLRegistry, AIVariant) and modified files should be consistent.
result: pass

## Summary

total: 8
passed: 1
issues: 0
pending: 0
skipped: 7

## Gaps

[none yet]
