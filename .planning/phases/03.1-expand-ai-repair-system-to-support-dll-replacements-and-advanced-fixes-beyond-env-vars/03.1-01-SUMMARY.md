---
phase: 03.1-expand-ai-repair-system
plan: 01
subsystem: core
tags: [wine, dll, github-api, winetricks, repair-loop, cnc-ddraw]

requires:
  - phase: 03-repair-loop
    provides: WineFix enum (3 cases), RetryVariant, LaunchCommand repair loop

provides:
  - WineFix enum expanded to 6 cases (placeDLL, setRegistry, compound added)
  - DLLPlacementTarget enum (gameDir, system32)
  - KnownDLLRegistry with cnc-ddraw entry (GitHub repo metadata)
  - DLLDownloader with cache-first GitHub release download and ditto zip extraction
  - WineActionExecutor dispatching all 6 WineFix cases to Wine mechanisms
  - CellarPaths.dllsDir / dllCacheDir / cachedDLLFile helpers
  - RetryVariant.actions optional field (backward-compatible)

affects:
  - 03.1-02 (AI prompt integration and LaunchCommand wiring consumes this infrastructure)
  - Any future plan referencing WineFix enum

tech-stack:
  added: []
  patterns:
    - "DispatchSemaphore + ResultBox @unchecked Sendable for synchronous URLSession (mirrors AIService)"
    - "Cache-first DLL download: check ~/.cellar/dlls/{name}/{file} before hitting network"
    - "ditto -xk for zip extraction (no SPM dependency, ships on all macOS)"
    - "WineActionExecutor: mutable envConfigs inout param, configIndex to mutate current slot"
    - "compound case: continue on partial failure (all sub-actions attempted)"

key-files:
  created:
    - Sources/cellar/Models/KnownDLLRegistry.swift
    - Sources/cellar/Core/DLLDownloader.swift
    - Sources/cellar/Core/WineActionExecutor.swift
  modified:
    - Sources/cellar/Core/WineErrorParser.swift
    - Sources/cellar/Models/Recipe.swift
    - Sources/cellar/Persistence/CellarPaths.swift
    - Sources/cellar/Core/AIService.swift
    - Sources/cellar/Commands/LaunchCommand.swift

key-decisions:
  - "cnc-ddraw only in KnownDLLRegistry v1 — dgVoodoo2 excluded (does not work on Wine per research)"
  - "Download failures are non-fatal: WineActionExecutor.execute() never throws, prints warning and returns false"
  - "placeDLL auto-applies requiredOverrides from KnownDLLRegistry (cnc-ddraw needs WINEDLLOVERRIDES=ddraw=n,b)"
  - "DirectDraw Init Failed pattern uses compound([placeDLL, setDLLOverride]) — natural fit for the new case"

patterns-established:
  - "KnownDLLRegistry.find(name:) lookup pattern for DLL metadata resolution"
  - "DLLDownloader.downloadAndCache + place two-step pattern for safe DLL placement"

requirements-completed: []

duration: 8min
completed: 2026-03-27
---

# Phase 03.1 Plan 01: Expanded Fix Action Infrastructure Summary

**WineFix enum extended to 6 cases with placeDLL/setRegistry/compound, cnc-ddraw DLL registry, GitHub release download with cache, and WineActionExecutor dispatcher — foundation for AI-driven DLL replacement repair**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-03-27T18:57:55Z
- **Completed:** 2026-03-27T19:05:00Z
- **Tasks:** 2
- **Files modified:** 8 (3 new, 5 modified)

## Accomplishments

- WineFix enum now supports placeDLL (with DLLPlacementTarget), setRegistry, and compound multi-step fixes
- KnownDLLRegistry provides static lookup for cnc-ddraw (FunkyFr3sh/cnc-ddraw GitHub repo)
- DLLDownloader fetches GitHub latest releases, extracts DLLs from zip via ditto, caches at ~/.cellar/dlls/
- WineActionExecutor dispatches all 6 WineFix cases — ready for Plan 02 wiring into LaunchCommand

## Task Commits

1. **Task 1: Expand WineFix, RetryVariant, CellarPaths, KnownDLLRegistry** - `937ccd5` (feat)
2. **Task 2: Create DLLDownloader and WineActionExecutor** - `7a7d3bc` (feat)

## Files Created/Modified

- `Sources/cellar/Core/WineErrorParser.swift` - DLLPlacementTarget enum, 3 new WineFix cases, DirectDraw pattern
- `Sources/cellar/Models/Recipe.swift` - RetryVariant.actions optional field
- `Sources/cellar/Persistence/CellarPaths.swift` - dllsDir, dllCacheDir, cachedDLLFile helpers
- `Sources/cellar/Models/KnownDLLRegistry.swift` (NEW) - KnownDLL struct and cnc-ddraw registry entry
- `Sources/cellar/Core/DLLDownloader.swift` (NEW) - GitHub release download, zip extraction, caching
- `Sources/cellar/Core/WineActionExecutor.swift` (NEW) - Dispatch WineFix to Wine mechanisms
- `Sources/cellar/Core/AIService.swift` - RetryVariant init updated for new actions field
- `Sources/cellar/Commands/LaunchCommand.swift` - Three switch statements extended for new WineFix cases

## Decisions Made

- cnc-ddraw only in KnownDLLRegistry for v1 — dgVoodoo2 excluded (does not work on Wine)
- Download failures are non-fatal: warnings printed, repair loop continues
- placeDLL auto-applies requiredOverrides so the caller doesn't need to know the DLL's WINEDLLOVERRIDES
- DirectDraw Init Failed detected as compound([placeDLL("cnc-ddraw", .gameDir), setDLLOverride("ddraw", "n,b")])

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Exhaustive switch errors in LaunchCommand.swift and AIService.swift**
- **Found during:** Task 1 (after expanding WineFix enum)
- **Issue:** Adding 3 new WineFix cases broke 3 existing switch statements in LaunchCommand.swift (exhaustive) and AIService.swift (RetryVariant initializer missing actions parameter)
- **Fix:** Extended all three LaunchCommand switch blocks with new cases (placeholder handling for advanced fixes); updated AIService RetryVariant call with `actions: nil`
- **Files modified:** Sources/cellar/Commands/LaunchCommand.swift, Sources/cellar/Core/AIService.swift
- **Verification:** `swift build` passes cleanly
- **Committed in:** 937ccd5 (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 - exhaustive switch)
**Impact on plan:** Required fix for build correctness. No scope creep — the new switch cases are forward-compatible stubs.

## Issues Encountered

None beyond the exhaustive switch auto-fix.

## Next Phase Readiness

- All infrastructure types compile and are ready for Plan 02
- WineActionExecutor awaits wiring into LaunchCommand's repair loop (Plan 02)
- AI prompt expansion to generate placeDLL/setRegistry actions (Plan 02)

---
*Phase: 03.1-expand-ai-repair-system*
*Completed: 2026-03-27*
