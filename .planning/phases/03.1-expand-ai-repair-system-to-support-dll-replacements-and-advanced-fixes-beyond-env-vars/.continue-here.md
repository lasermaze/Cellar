---
phase: 03.1-expand-ai-repair-system
task: UAT runtime testing
total_tasks: 2 bugs to fix
status: bugs_found
last_updated: 2026-03-27T21:11:54.341Z
---

<current_state>
Phase 03.1 code is COMPLETE and compiles. All plans executed, verification passed. But runtime testing revealed two bugs when launching Cossacks (`swift run cellar launch cossacks-european-wars`):

**Bug 1: AIServiceError prints as "error 0"**
- `AIServiceError` enum doesn't implement `LocalizedError`, so `error.localizedDescription` returns "The operation couldn't be completed. (cellar.AIServiceError error 0.)" instead of useful info like "HTTP 401" or "all retries failed"
- Location: `Sources/cellar/Models/AIModels.swift:13` — `enum AIServiceError: Error`
- Fix: Add `LocalizedError` conformance with `errorDescription` computed property

**Bug 2: Need to verify cnc-ddraw bundled retry variant was attempted**
- The Cossacks recipe was updated with cnc-ddraw as first retry variant (`recipes/cossacks-european-wars.json`)
- User output shows DirectDraw Init Failed, then jumps straight to AI escalation messages
- Need to check: was the cnc-ddraw retry variant from the recipe actually tried? The output doesn't show DLL download/placement messages
- Investigate: `LaunchCommand.swift` — how `RetryVariant.actions` are loaded and applied, whether `WineActionExecutor` is called for recipe-level actions before AI kicks in

**User's actual output:**
```
Did the game reach the menu? [y/n] n
What did you see? Direct Draw Init Failed (80004001)

Trying environment variable adjustments...
AI variant generation failed: The operation couldn't be completed. (cellar.AIServiceError error 0.)

Escalating to DLL overrides and winetricks...
AI variant generation failed: The operation couldn't be completed. (cellar.AIServiceError error 0.)

Escalating to DLL replacements and registry edits...
AI variant generation failed: The operation couldn't be completed. (cellar.AIServiceError error 0.)
```
</current_state>

<completed_work>
- Phase 03.1 fully planned, executed, and verified (2 plans, 2 waves)
- Plan 01: WineFix expanded to 6 cases, KnownDLLRegistry, DLLDownloader, WineActionExecutor
- Plan 02: Progressive AI prompts (L1/L2/L3), LaunchCommand escalation wiring, Cossacks cnc-ddraw recipe
- Build compiles cleanly (`swift build` passes)
- UAT created with 8 tests — 1 passed (build), 7 skipped (need runtime)
- User ran `cellar launch cossacks-european-wars` and hit the two bugs above
</completed_work>

<remaining_work>
1. Fix AIServiceError to implement LocalizedError (quick fix in AIModels.swift)
2. Debug why cnc-ddraw retry variant doesn't appear to run — check LaunchCommand retry loop for recipe action loading/execution
3. Re-test with Cossacks after fixes
4. Also check: does user have ANTHROPIC_API_KEY set? The "error 0" might be masking a real auth/network error
</remaining_work>

<decisions_made>
- Phase 03.1 context decisions all captured in 03.1-CONTEXT.md (graduated escalation, DLL downloads from GitHub, cnc-ddraw first-class support, structured action schema, etc.)
- All implementation decisions in STATE.md accumulated context section
</decisions_made>

<blockers>
- The AI API call failure might be an API key issue or a real bug in the HTTP call — the unhelpful error message masks the root cause
- cnc-ddraw variant may not be executing due to a wiring issue in how RetryVariant.actions are parsed and dispatched
</blockers>

<context>
The whole point of Phase 03.1 was to fix Cossacks' DirectDraw Init Failed error by letting the AI suggest cnc-ddraw (and other advanced fixes). The infrastructure is built, but the runtime path has bugs. The most likely quick win: fix the error messages first (AIServiceError), then debug why cnc-ddraw isn't being tried from the bundled recipe.
</context>

<next_action>
1. Fix AIServiceError in AIModels.swift — add LocalizedError conformance
2. Add debug prints or read LaunchCommand.swift retry loop to trace whether recipe RetryVariant.actions are being parsed and executed
3. Check if user has API key set (the error might just be "unavailable" hitting the wrong code path)
4. Re-run `swift run cellar launch cossacks-european-wars` and verify cnc-ddraw download + placement
</next_action>
