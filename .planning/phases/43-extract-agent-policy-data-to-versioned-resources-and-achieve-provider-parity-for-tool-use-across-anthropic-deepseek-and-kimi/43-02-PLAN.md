---
phase: 43-extract-agent-policy-data-to-versioned-resources-and-achieve-provider-parity-for-tool-use-across-anthropic-deepseek-and-kimi
plan: 02
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - Sources/cellar/Core/AIService.swift
  - Sources/cellar/Core/Tools/ConfigTools.swift
  - Sources/cellar/Models/EngineRegistry.swift
  - Sources/cellar/Models/KnownDLLRegistry.swift
  - Sources/cellar/Core/AgentToolName.swift
  - Sources/cellar/Core/CollectiveMemoryService.swift
autonomous: true
requirements: [POL-01, POL-02, POL-03, POL-04]

must_haves:
  truths:
    - "AIService.swift agent-loop system prompt is sourced from PolicyResources.shared.systemPrompt — the Swift literal is gone"
    - "AgentTools.allowedEnvKeys delegates to PolicyResources.shared.envAllowlist (computed)"
    - "AgentTools.allowedRegistryPrefixes delegates to PolicyResources.shared.registryAllowlist (computed)"
    - "EngineRegistry.engines and KnownDLLRegistry.registry are computed properties backed by PolicyResources"
    - "AgentToolName.metadata input schemas read from PolicyResources.shared.toolSchemas — inline JSONValue literals removed"
    - "All existing call sites compile unchanged (no API surface change for the three registries) — agent sessions still launch and run"
  artifacts:
    - path: "Sources/cellar/Core/AIService.swift"
      provides: "Agent-loop system prompt now reads from PolicyResources"
      contains: "PolicyResources.shared.systemPrompt"
    - path: "Sources/cellar/Core/Tools/ConfigTools.swift"
      provides: "Env + registry allowlists delegate to PolicyResources"
      contains: "PolicyResources.shared"
    - path: "Sources/cellar/Models/EngineRegistry.swift"
      provides: "engines computed property delegating to PolicyResources"
      contains: "PolicyResources.shared.engineDefinitions"
    - path: "Sources/cellar/Models/KnownDLLRegistry.swift"
      provides: "registry computed property delegating to PolicyResources"
      contains: "PolicyResources.shared.dllRegistry"
    - path: "Sources/cellar/Core/AgentToolName.swift"
      provides: "Tool inputSchema sourced from PolicyResources.shared.toolSchemas"
      contains: "PolicyResources.shared.toolSchemas"
  key_links:
    - from: "Sources/cellar/Core/AIService.swift"
      to: "PolicyResources.shared.systemPrompt"
      via: "direct property read in agent-loop prompt builder"
      pattern: "PolicyResources\\.shared\\.systemPrompt"
    - from: "Sources/cellar/Core/Tools/ConfigTools.swift"
      to: "PolicyResources.shared.envAllowlist"
      via: "computed AgentTools.allowedEnvKeys"
      pattern: "PolicyResources\\.shared\\.envAllowlist"
    - from: "Sources/cellar/Core/AgentToolName.swift"
      to: "PolicyResources.shared.toolSchemas"
      via: "metadata.inputSchema lookup by raw value"
      pattern: "toolSchemas\\["
---

<objective>
Rewire every existing call site that previously read inline policy data so it now reads from `PolicyResources.shared`. Five Swift files change; the literal arrays/strings are deleted. No new public API is introduced beyond what `PolicyResources` exposed in 43-01. Behavior must be byte-identical to before — same prompt, same allowlists, same engines, same DLLs, same tool schemas.

Purpose: Complete the policy-extraction migration so future edits go to `Resources/policy/*` instead of Swift literals. Closes POL-01..04.

Output: AIService, ConfigTools, EngineRegistry, KnownDLLRegistry, and AgentToolName all consume `PolicyResources.shared`. CollectiveMemoryService also reads its registry-prefix list from PolicyResources (it duplicates this list today — see 43-RESEARCH pitfall 3 / Open Question 2 / `CollectiveMemoryService.swift:423`).
</objective>

<execution_context>
@/Users/peter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-extract-agent-policy-data-to-versioned-resources-and-achieve-provider-parity-for-tool-use-across-anthropic-deepseek-and-kimi/43-CONTEXT.md
@.planning/phases/43-extract-agent-policy-data-to-versioned-resources-and-achieve-provider-parity-for-tool-use-across-anthropic-deepseek-and-kimi/43-RESEARCH.md
@.planning/phases/43-extract-agent-policy-data-to-versioned-resources-and-achieve-provider-parity-for-tool-use-across-anthropic-deepseek-and-kimi/43-01-SUMMARY.md

@Sources/cellar/Core/PolicyResources.swift
@Sources/cellar/Core/AIService.swift
@Sources/cellar/Core/Tools/ConfigTools.swift
@Sources/cellar/Models/EngineRegistry.swift
@Sources/cellar/Models/KnownDLLRegistry.swift
@Sources/cellar/Core/AgentToolName.swift
@Sources/cellar/Core/CollectiveMemoryService.swift

<interfaces>
<!-- Public surface produced by 43-01. Use directly. No exploration needed. -->

From Sources/cellar/Core/PolicyResources.swift (created in 43-01):
```swift
struct PolicyResources {
    static let shared: PolicyResources

    let systemPrompt: String                    // agent-loop prompt body
    let engineDefinitions: [EngineDefinition]
    let dllRegistry: [KnownDLL]
    let envAllowlist: Set<String>
    let registryAllowlist: [String]
    let toolSchemas: [String: JSONValue]        // keyed by AgentToolName.rawValue
}
```

Existing call sites to rewire (verified by grep):
- `Sources/cellar/Core/AIService.swift` — `let systemPrompt = """ ... """` near line 673 (agent-loop builder ONLY).
- `Sources/cellar/Core/Tools/ConfigTools.swift:11` — `static let allowedEnvKeys: Set<String> = [...]`.
- `Sources/cellar/Core/Tools/ConfigTools.swift:55` — `private static let allowedRegistryPrefixes: [String] = [...]`.
- `Sources/cellar/Models/EngineRegistry.swift` — `static let engines: [EngineDefinition]`.
- `Sources/cellar/Models/KnownDLLRegistry.swift` — `static let registry: [KnownDLL]`.
- `Sources/cellar/Core/AgentToolName.swift` — `inputSchema: JSONValue` literals inside metadata table per case.
- `Sources/cellar/Core/CollectiveMemoryService.swift:423` — duplicate `let allowedRegistryPrefixes: [String] = ["HKEY_CURRENT_USER\\\\", "HKEY_LOCAL_MACHINE\\\\"]` (note: this list is shorter/different than ConfigTools's; replace with `PolicyResources.shared.registryAllowlist` to unify).

DO NOT change any other prompt site (AIService.diagnose / generateRecipe / generateVariants stay inline — those are short context-specific prompts, not policy).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire AIService system prompt + AgentTools allowlists</name>
  <files>
    Sources/cellar/Core/AIService.swift,
    Sources/cellar/Core/Tools/ConfigTools.swift,
    Sources/cellar/Core/CollectiveMemoryService.swift
  </files>
  <action>
1. **AIService.swift** — replace the agent-loop system prompt:
   - Find the long `let systemPrompt = """ ... """` block in the agent-loop builder (the ~270-line one near line 673 that was migrated to `Resources/policy/system_prompt.md` in 43-01).
   - Replace with: `let systemPrompt = PolicyResources.shared.systemPrompt`
   - Delete the entire literal string.
   - DO NOT touch the shorter prompts in `_diagnose`, `_generateRecipe`, `_generateVariants` (around lines 240, 335, 462). Those stay inline.

2. **ConfigTools.swift** — turn allowlist statics into computed delegators:
   - Change `static let allowedEnvKeys: Set<String> = [...]` to:
     ```swift
     static var allowedEnvKeys: Set<String> { PolicyResources.shared.envAllowlist }
     ```
   - Change `private static let allowedRegistryPrefixes: [String] = [...]` to:
     ```swift
     static var allowedRegistryPrefixes: [String] { PolicyResources.shared.registryAllowlist }
     ```
     (Drop `private` — CollectiveMemoryService needs to read it via the same computed surface, OR keep private and have CollectiveMemoryService read `PolicyResources.shared.registryAllowlist` directly. Choose the latter to keep `ConfigTools` private surface unchanged.)
   - Update existing call sites in this file (`AgentTools.allowedEnvKeys.contains(...)` etc.) — call sites unchanged because the property name and type match.
   - Delete the inline literal arrays.

3. **CollectiveMemoryService.swift** — remove the duplicate registry prefix list:
   - At line ~423 (`let allowedRegistryPrefixes: [String] = ["HKEY_CURRENT_USER\\\\", "HKEY_LOCAL_MACHINE\\\\"]`), replace with:
     ```swift
     let allowedRegistryPrefixes = PolicyResources.shared.registryAllowlist
     ```
     The behavior tightens slightly (longer prefixes). That's the intended unification — `registry_allowlist.json` is the single source. If the test suite catches a divergence, treat it as a real bug to fix in the JSON, not by reverting.
   - The env-key sanitization at line 400 already uses `AgentTools.allowedEnvKeys` — it now flows through PolicyResources automatically.

DO NOT change any other file in this task.
DO NOT change `Set<String>` to `Array<String>` or vice versa — types must match the existing call sites byte-for-byte.
DO NOT delete `import Foundation` or other imports unless an unused warning appears.
  </action>
  <verify>
    <automated>swift build 2>&1 | tail -15 && swift test --filter "SecurityTests|CollectiveMemoryEntryTests|SettingsTests" 2>&1 | tail -10 && grep -c 'let systemPrompt = """' Sources/cellar/Core/AIService.swift | grep -E '^[0-3]$' </automated>
  </verify>
  <done>`swift build` is green. The long agent-loop literal is gone from `AIService.swift`. `AgentTools.allowedEnvKeys` and `allowedRegistryPrefixes` are computed delegators. `CollectiveMemoryService` no longer hardcodes its registry prefix list. Targeted tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire EngineRegistry, KnownDLLRegistry, AgentToolName schemas</name>
  <files>
    Sources/cellar/Models/EngineRegistry.swift,
    Sources/cellar/Models/KnownDLLRegistry.swift,
    Sources/cellar/Core/AgentToolName.swift
  </files>
  <action>
1. **EngineRegistry.swift** — make `engines` a computed delegator:
   - Replace `static let engines: [EngineDefinition] = [ ... ]` with:
     ```swift
     static var engines: [EngineDefinition] { PolicyResources.shared.engineDefinitions }
     ```
   - Delete the literal array.
   - Keep ALL detection / matching functions (`detect(...)` etc.) unchanged — only the data source moves.
   - If `EngineDefinition` was extended with Codable in 43-01: leave that conformance in place. If 43-01 kept mapping local to `PolicyResources.swift`, do not touch the struct here.

2. **KnownDLLRegistry.swift** — make `registry` a computed delegator:
   - Replace `static let registry: [KnownDLL] = [ ... ]` with:
     ```swift
     static var registry: [KnownDLL] { PolicyResources.shared.dllRegistry }
     ```
   - Delete the literal array.
   - Keep all helper methods (`find(by:)`, etc.) unchanged.

3. **AgentToolName.swift** — replace inline `inputSchema` literals with PolicyResources lookup:
   - Each `case .xxx` returns a `ToolMetadata` with an `inputSchema: JSONValue` field. Replace each inline schema literal with a lookup:
     ```swift
     // before:
     inputSchema: .object([ "type": .string("object"), "properties": .object([...]), "required": .array([...]) ]),
     // after (per case):
     inputSchema: PolicyResources.shared.toolSchemas[Self.<case>.rawValue] ?? .object(["type": .string("object")])
     ```
   - To avoid repetition, factor a small helper inside the type:
     ```swift
     private static func schema(for tool: AgentToolName) -> JSONValue {
         PolicyResources.shared.toolSchemas[tool.rawValue]
             ?? .object(["type": .string("object"), "properties": .object([:]), "required": .array([])])
     }
     ```
     and use `inputSchema: Self.schema(for: .inspectGame)` per case. The fallback only fires if a tool is missing from `tool_schemas.json` — that's a build-error in spirit; default to a minimally valid schema rather than a fatal so unit tests still run.
   - Verify: every `AgentToolName.allCases` raw value appears as a key in `tool_schemas.json` (43-01 requirement). If 43-01 missed any case, fix `tool_schemas.json` (do not work around it here).
   - Delete the inline schema literals.

DO NOT change `ToolMetadata` shape, the `description` strings, or the `pendingActionDescription` callbacks.
DO NOT make `metadata` async — `PolicyResources.shared` is fully eager-loaded.
  </action>
  <verify>
    <automated>swift build 2>&1 | tail -15 && swift test --filter "EngineRegistryTests|KnownDLLRegistryExpandedTests|AgentToolDefinitionTests" 2>&1 | tail -15</automated>
  </verify>
  <done>The three files compile. `EngineRegistry.engines` and `KnownDLLRegistry.registry` are computed properties. Every `AgentToolName.metadata.inputSchema` resolves via `PolicyResources.shared.toolSchemas`. Existing tests `EngineRegistryTests`, `KnownDLLRegistryExpandedTests`, `AgentToolDefinitionTests` pass without modification.</done>
</task>

</tasks>

<verification>
- `swift build` is green; no warnings introduced.
- Full test suite: `swift test 2>&1 | tail -25` shows green or only previously-known failures (none for these subsystems).
- Smoke test (manual / optional): start `cellar serve`, launch a known-working game, confirm agent system prompt logs the same first line as before.
- `grep -n 'let systemPrompt = """' Sources/cellar/Core/AIService.swift` returns at most three short prompt-builder calls (diagnose / recipe / variants), NOT the long agent-loop one.
- `grep -rn 'allowedEnvKeys: Set<String> = \[' Sources/cellar` returns zero matches.
- `grep -rn 'static let registry: \[KnownDLL\] = \[' Sources/cellar` returns zero matches.
- `grep -rn 'static let engines: \[EngineDefinition\] = \[' Sources/cellar` returns zero matches.
</verification>

<success_criteria>
- All five inline policy literals (system prompt, env allowlist, registry allowlist, engines, DLL registry, tool schemas) are deleted from their original Swift files.
- All call sites compile and tests pass — type signatures and call-site syntax of the public surface are unchanged.
- `AgentToolName.metadata.inputSchema` for every `allCases` resolves to a non-fallback value via `PolicyResources.shared.toolSchemas`.
- `CollectiveMemoryService` registry-prefix duplicate list is gone — it reads `PolicyResources.shared.registryAllowlist`.
- `swift build` and `swift test` both green.
</success_criteria>

<output>
After completion, create `.planning/phases/43-extract-agent-policy-data-to-versioned-resources-and-achieve-provider-parity-for-tool-use-across-anthropic-deepseek-and-kimi/43-02-SUMMARY.md` documenting: (a) every file edited and the LOC removed/added per file, (b) any tool whose `inputSchema` had to fall back to the default (signals a missing key in `tool_schemas.json`), (c) any test that broke and the diagnosis, (d) any divergence found between `CollectiveMemoryService`'s old short list and the unified `registry_allowlist.json`.
</output>
