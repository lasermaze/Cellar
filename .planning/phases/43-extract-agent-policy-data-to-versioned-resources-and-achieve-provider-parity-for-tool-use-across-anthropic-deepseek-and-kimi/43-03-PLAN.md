---
phase: 43-extract-agent-policy-data-to-versioned-resources-and-achieve-provider-parity-for-tool-use-across-anthropic-deepseek-and-kimi
plan: 03
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - Sources/cellar/Models/AgentToolCall.swift
  - Sources/cellar/Core/AgentProvider.swift
  - Sources/cellar/Core/Providers/AnthropicAdapter.swift
  - Sources/cellar/Core/Providers/DeepseekAdapter.swift
  - Sources/cellar/Core/Providers/KimiAdapter.swift
  - Sources/cellar/Core/AgentLoop.swift
  - Sources/cellar/Core/AgentTools.swift
  - Tests/cellarTests/Providers/AnthropicAdapterTests.swift
  - Tests/cellarTests/Providers/DeepseekAdapterTests.swift
  - Tests/cellarTests/Providers/KimiAdapterTests.swift
autonomous: true
requirements: [TUP-01, TUP-02, TUP-03, TUP-04, TUP-05]

must_haves:
  truths:
    - "All three adapters (Anthropic, DeepSeek, Kimi) expose AgentToolCall through AgentLoopProviderResponse — no anonymous tuple anywhere"
    - "Every adapter has unit tests verifying encode/decode round-trip on a fixture set of AgentToolCall values"
    - "DeepSeek and Kimi route all 24 tools through native OpenAI-compat tool_calls — no JSON-in-text fallback"
    - "Anthropic adapter continues to use tool_use / tool_result blocks unchanged in semantics"
    - "AgentLoop and AgentTools consume AgentToolCall (not the tuple) — rest of codebase sees only the canonical shape"
  artifacts:
    - path: "Sources/cellar/Models/AgentToolCall.swift"
      provides: "Canonical AgentToolCall struct {id, name, input}"
      min_lines: 15
      contains: "struct AgentToolCall"
    - path: "Sources/cellar/Core/AgentProvider.swift"
      provides: "AgentLoopProviderResponse.toolCalls retyped to [AgentToolCall]"
      contains: "[AgentToolCall]"
    - path: "Sources/cellar/Core/Providers/AnthropicAdapter.swift"
      provides: "Decodes tool_use blocks into [AgentToolCall], encodes tool_result via appendToolResults"
      contains: "AgentToolCall("
    - path: "Sources/cellar/Core/Providers/DeepseekAdapter.swift"
      provides: "Decodes OpenAI tool_calls into [AgentToolCall], encodes tool role messages"
      contains: "AgentToolCall("
    - path: "Sources/cellar/Core/Providers/KimiAdapter.swift"
      provides: "Decodes OpenAI tool_calls into [AgentToolCall], encodes tool role messages"
      contains: "AgentToolCall("
    - path: "Tests/cellarTests/Providers/AnthropicAdapterTests.swift"
      provides: "Round-trip + tool_use_id preservation + multi-call decode tests"
      min_lines: 40
    - path: "Tests/cellarTests/Providers/DeepseekAdapterTests.swift"
      provides: "Round-trip + tool_call_id preservation + arguments JSON parse tests"
      min_lines: 40
    - path: "Tests/cellarTests/Providers/KimiAdapterTests.swift"
      provides: "Round-trip + tool_call_id preservation + arguments JSON parse tests"
      min_lines: 40
  key_links:
    - from: "Sources/cellar/Core/Providers/AnthropicAdapter.swift"
      to: "AgentToolCall"
      via: "translateAnthropicResponse builds [AgentToolCall]"
      pattern: "AgentToolCall\\("
    - from: "Sources/cellar/Core/Providers/DeepseekAdapter.swift"
      to: "AgentToolCall"
      via: "translateDeepseekResponse builds [AgentToolCall]"
      pattern: "AgentToolCall\\("
    - from: "Sources/cellar/Core/Providers/KimiAdapter.swift"
      to: "AgentToolCall"
      via: "translateKimiResponse builds [AgentToolCall]"
      pattern: "AgentToolCall\\("
    - from: "Sources/cellar/Core/AgentLoop.swift"
      to: "AgentToolCall"
      via: "iterates response.toolCalls (now typed)"
      pattern: "for .* in .*\\.toolCalls"
---

<objective>
Replace the anonymous `(id: String, name: String, input: JSONValue)` tuple used inside `AgentLoopProviderResponse.toolCalls` and the three adapters with a canonical `AgentToolCall` struct. Add encode/decode round-trip unit tests for each adapter so DeepSeek and Kimi achieve documented behavioral parity with Anthropic for native function-calling. The wire-protocol seam stays at the adapter boundary (Phase 42 invariant); `AgentLoop` and `AgentTools` see only the canonical shape.

Purpose: Closes TUP-01..05 in one atomic, testable change. Sets up Phase 44/45 to refactor memory/sessions without touching the wire layer again.

Output: One new struct, one renamed protocol field, three updated adapters, and three new test files. No new dependencies. No JSON-in-text fallback path.
</objective>

<execution_context>
@/Users/peter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-extract-agent-policy-data-to-versioned-resources-and-achieve-provider-parity-for-tool-use-across-anthropic-deepseek-and-kimi/43-CONTEXT.md
@.planning/phases/43-extract-agent-policy-data-to-versioned-resources-and-achieve-provider-parity-for-tool-use-across-anthropic-deepseek-and-kimi/43-RESEARCH.md

@Sources/cellar/Core/AgentProvider.swift
@Sources/cellar/Core/Providers/AnthropicAdapter.swift
@Sources/cellar/Core/Providers/DeepseekAdapter.swift
@Sources/cellar/Core/Providers/KimiAdapter.swift
@Sources/cellar/Core/AgentLoop.swift
@Sources/cellar/Core/AgentTools.swift
@Sources/cellar/Models/AIModels.swift

<interfaces>
<!-- Existing tuple shape used everywhere — replace atomically. -->

From Sources/cellar/Core/AgentProvider.swift (Phase 42):
```swift
struct AgentLoopProviderResponse {
    let stopReason: AgentStopReason
    let text: String?
    let toolCalls: [(id: String, name: String, input: JSONValue)]   // ← becomes [AgentToolCall]
    let usage: TokenUsage
    // ...other fields unchanged
}
```

Adapter call sites (verified):
- AnthropicAdapter.swift:117 — `var toolCalls: [(id: String, name: String, input: JSONValue)] = []`
- AnthropicAdapter.swift:140 — `toolCalls: toolCalls,` in response build
- DeepseekAdapter.swift:171 / 201 — same pattern
- KimiAdapter.swift:168 / 198 — same pattern
- AnthropicAdapter.swift:36 / DeepseekAdapter.swift:67 / KimiAdapter.swift:65 — `appendAssistantResponse(_ response:)` reads `response.toolCalls` and re-encodes for wire
- AnthropicAdapter.swift:47 / DeepseekAdapter.swift:93 / KimiAdapter.swift:90 — `appendToolResults(_ results: [(id: String, content: String, isError: Bool)])` (KEEP this tuple — it is the result wire input shape, not a tool call; out of scope for TUP-01)

AgentLoop / AgentTools consumers:
- AgentLoop.swift iterates `response.toolCalls` and forwards `(id, name, input)` to AgentTools.execute. Update the iteration to use `.id`, `.name`, `.input` properties.
- AgentTools.swift’s execute() signature already takes `(id: String, name: String, input: JSONValue)` or similar — verify exact signature in the file and update to accept `AgentToolCall` if needed (or destructure at call site).

JSONValue conformance (already in codebase):
- `JSONValue` is already `Codable` and `Sendable`. No changes needed.

Wire-protocol entries:
- Anthropic `tool_use`: emits content blocks `{type: "tool_use", id, name, input}`. `tool_result`: `{type: "tool_result", tool_use_id, content, is_error}`. (Already implemented in AnthropicAdapter.)
- OpenAI-compat (DeepSeek, Kimi): emits `tool_calls: [{id, type: "function", function: {name, arguments: <stringified JSON>}}]`. Tool result is a `{role: "tool", tool_call_id, content}` message. (Already implemented in both adapters.)
- The `arguments` field is a JSON-stringified payload — `JSONEncoder().encode(jsonValue)` produces it correctly because JSONValue is Codable. Decoding uses `JSONDecoder().decode(JSONValue.self, from: argumentsData)` (already in the existing code).

Existing test framework:
- `import Testing`. Use `@Test` and `#expect(...)`. See `Tests/cellarTests/AgentToolDefinitionTests.swift` for the project pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce AgentToolCall + retype AgentLoopProviderResponse + update three adapters atomically</name>
  <files>
    Sources/cellar/Models/AgentToolCall.swift,
    Sources/cellar/Core/AgentProvider.swift,
    Sources/cellar/Core/Providers/AnthropicAdapter.swift,
    Sources/cellar/Core/Providers/DeepseekAdapter.swift,
    Sources/cellar/Core/Providers/KimiAdapter.swift,
    Sources/cellar/Core/AgentLoop.swift,
    Sources/cellar/Core/AgentTools.swift
  </files>
  <action>
This is a single atomic refactor — partial application leaves the build broken. Apply ALL of the following in one task:

1. **Create `Sources/cellar/Models/AgentToolCall.swift`**:
   ```swift
   import Foundation

   /// Provider-neutral representation of a single tool invocation emitted by the model.
   /// Adapters translate this <-> wire protocol (Anthropic tool_use blocks / OpenAI tool_calls).
   /// The `id` is opaque: it carries `tool_use_id` (Anthropic) or `tool_call_id` (OpenAI-compat).
   struct AgentToolCall: Sendable, Equatable {
       let id: String
       let name: String
       let input: JSONValue
   }
   ```
   `Equatable` is needed for unit-test round-trip assertions. JSONValue must already be Equatable in the codebase — if not, add the conformance in this same file (a small extension is fine; if not feasible, drop Equatable and compare via JSON encoding in tests).

2. **`Sources/cellar/Core/AgentProvider.swift`**: change `AgentLoopProviderResponse.toolCalls` field type:
   - From: `let toolCalls: [(id: String, name: String, input: JSONValue)]`
   - To:   `let toolCalls: [AgentToolCall]`
   No other field changes.

3. **`AnthropicAdapter.swift`**:
   - In `translateAnthropicResponse`: change local `var toolCalls: [(id: String, name: String, input: JSONValue)] = []` to `var toolCalls: [AgentToolCall] = []`.
   - The `case .toolUse(let id, let name, let input):` branch should append `AgentToolCall(id: id, name: name, input: input)`.
   - In `appendAssistantResponse`: code that iterates `response.toolCalls` and emits `tool_use` content blocks now uses `.id`, `.name`, `.input` accessors (not tuple labels — though tuple-with-labels and struct accessors are spelled the same in Swift, the change is type-only).
   - `appendToolResults` is unchanged — it operates on tool results, not tool calls.

4. **`DeepseekAdapter.swift`**:
   - In `translateDeepseekResponse`: change local declaration to `var toolCalls: [AgentToolCall] = []`. Replace tuple append with `AgentToolCall(id: call.id, name: call.function.name, input: input)` where `input` is the existing `JSONDecoder().decode(JSONValue.self, from: argumentsData)` result.
   - In `appendAssistantResponse`: when building `[OpenAIToolRequest.ToolCall]` from `response.toolCalls`, use `.id`, `.name`, `.input` and stringify `input` via `JSONEncoder().encode(input)` to produce the `arguments` String. (This logic almost certainly already exists — only the iteration field type changes.)

5. **`KimiAdapter.swift`**: same as DeepseekAdapter — the wire protocol is identical (OpenAI-compat). Mirror the changes.

6. **`AgentLoop.swift`**: find every spot that destructures the old tuple and update:
   - Old: `for (id, name, input) in response.toolCalls { ... }` (or `for call in response.toolCalls { call.id }` — Swift label tuples support member-style access)
   - New: `for call in response.toolCalls { /* use call.id, call.name, call.input */ }`

7. **`AgentTools.swift`**: if `execute(name: String, input: JSONValue, id: String) -> ToolResult` is called by AgentLoop with the destructured tuple, no signature change is required — the call site simply passes `call.name`, `call.input`, `call.id`. If the existing signature ALREADY accepts a tuple, replace its parameter with `AgentToolCall`. (Inspect first; minimize surface area.)

DO NOT change `appendToolResults`'s parameter shape `[(id: String, content: String, isError: Bool)]` — tool RESULTS are out of scope for TUP-01..04. They might warrant an `AgentToolResult` struct in a future phase; not now.
DO NOT introduce a JSON-in-text fallback path. DeepSeek and Kimi must use native `tool_calls` only. If existing code has any "parse JSON out of text content" branch for tool calls, delete it — CONTEXT.md locks "no degraded mode."
DO NOT change provider HTTP wire contracts beyond replacing tuples with `AgentToolCall` field accesses.
DO NOT alter `AgentToolName` or `PolicyResources` — those are 43-01 / 43-02 territory.

Suggested commit shape: one atomic commit `refactor(43-03): introduce AgentToolCall struct and retype provider response`. The build must be green after this single commit.
  </action>
  <verify>
    <automated>swift build 2>&1 | tail -25 && swift test --filter "AgentToolDefinitionTests" 2>&1 | tail -10 && grep -rn 'toolCalls: \[(id: String, name: String, input: JSONValue)\]' Sources/cellar | wc -l | grep -q '^\s*0\s*$'</automated>
  </verify>
  <done>The tuple type `[(id: String, name: String, input: JSONValue)]` has zero matches in `Sources/cellar`. `AgentToolCall` struct exists and is used in all three adapters and in `AgentLoopProviderResponse`. `swift build` is green. Existing tests pass without modification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adapter round-trip tests — encode/decode parity per provider</name>
  <files>
    Tests/cellarTests/Providers/AnthropicAdapterTests.swift,
    Tests/cellarTests/Providers/DeepseekAdapterTests.swift,
    Tests/cellarTests/Providers/KimiAdapterTests.swift
  </files>
  <behavior>
    Each adapter test file covers the same three behaviors with provider-specific fixtures:
    - Test A (decode response): given a representative provider HTTP response JSON containing one or two tool calls, the adapter's response translator returns `[AgentToolCall]` whose `id`, `name`, and `input` exactly match the wire payload (including nested JSONValue equality).
    - Test B (encode tool_call back into request payload via appendAssistantResponse): given an `AgentLoopProviderResponse` with two `AgentToolCall` values, the adapter emits assistant-message content (Anthropic tool_use blocks / OpenAI-compat tool_calls) where every id, name, and stringified arguments matches the input.
    - Test C (round-trip): `decode(encode(call)) == call` for a fixture set of three calls including one with nested object input, one with array input, and one with empty input.
  </behavior>
  <action>
Create three new test files under `Tests/cellarTests/Providers/`. Use `import Testing` and `@testable import cellar`.

**Common fixture set (define a private helper in each file or share via a small `Tests/cellarTests/Providers/AdapterTestFixtures.swift`)**:
```swift
let calls: [AgentToolCall] = [
    AgentToolCall(id: "call_1", name: "inspect_game", input: .object([:])),
    AgentToolCall(id: "call_2", name: "set_environment", input: .object([
        "key": .string("WINEDLLOVERRIDES"), "value": .string("ddraw=n,b")
    ])),
    AgentToolCall(id: "call_3", name: "search_web", input: .object([
        "query": .string("starcraft wine"), "tags": .array([.string("rts"), .string("blizzard")])
    ])),
]
```

**`AnthropicAdapterTests.swift`** — three @Tests:
1. `decodesToolUseBlocks` — feed a JSON Anthropic response body containing two `{type: "tool_use", id, name, input}` blocks into the adapter's response translator. Assert the result `.toolCalls` array equals two `AgentToolCall` values with matching fields.
2. `encodesToolUseInAssistantTurn` — call `appendAssistantResponse` with an `AgentLoopProviderResponse` whose `toolCalls = calls`. Snapshot the resulting message-array state (or expose a helper to dump it). Assert the encoded `tool_use` blocks have ids `call_1`, `call_2`, `call_3` and inputs that decode back to the original JSONValue.
3. `roundTrip` — run encode → decode and assert `calls == decoded`.

**`DeepseekAdapterTests.swift`** — three @Tests:
1. `decodesOpenAIToolCalls` — feed a JSON DeepSeek response with `tool_calls: [{id, type: "function", function: {name, arguments: "<json string>"}}]`. Assert two `AgentToolCall` values are produced and `arguments` JSON parses correctly into the expected `JSONValue`.
2. `encodesToolCallsInAssistantMessage` — call `appendAssistantResponse` and assert the emitted assistant message has `tool_calls` whose stringified `arguments` round-trip back to the original input.
3. `roundTrip` — same as Anthropic.

**`KimiAdapterTests.swift`** — three @Tests, mirror DeepseekAdapterTests verbatim with `KimiAdapter` types and any Kimi-specific response shape (the wire shape is the same OpenAI-compat protocol, but the model field and HTTP base URL differ).

If an adapter's translator method is not directly testable (e.g. it consumes a full HTTP response object), expose an `internal` helper that takes a parsed JSON payload and returns the translated response. That keeps tests deterministic — no HTTP needed.

DO NOT make these tests perform real HTTP calls. Use hand-written JSON fixtures inline in the test file. (CONTEXT.md left fixture-format as Claude's discretion — pick hand-written for determinism.)
DO NOT test parallel tool calls — out of scope per CONTEXT.md.
DO NOT depend on `PolicyResources` from 43-01/02 in adapter tests — these tests run in parallel with 43-02. Adapter behavior is independent of policy resources.

Each test file: ~40–80 LOC including fixtures.
  </action>
  <verify>
    <automated>swift test --filter "AnthropicAdapterTests" 2>&1 | tail -10 && swift test --filter "DeepseekAdapterTests" 2>&1 | tail -10 && swift test --filter "KimiAdapterTests" 2>&1 | tail -10</automated>
  </verify>
  <done>All three test files exist with at least 3 `@Test` cases each. `swift test --filter "AdapterTests"` (or per-provider filter) is green. The round-trip test specifically asserts `decoded == calls` for nested-object and array inputs in every provider.</done>
</task>

</tasks>

<verification>
- `swift build` is green; no warnings.
- `swift test 2>&1 | tail -25` shows all tests pass, including the three new adapter test files.
- `grep -rn 'toolCalls: \[(id:' Sources/cellar Tests/cellarTests` returns zero matches — the tuple is gone.
- `grep -rn 'AgentToolCall(' Sources/cellar Tests/cellarTests` returns matches in all three adapters and all three test files.
- Manual smoke (recommended but optional): launch one game per provider end-to-end. Pre-launch checklist: AI_PROVIDER=anthropic, then deepseek, then kimi, with a known-working game. Confirm tool calls are emitted natively (no `Failed to parse tool call from text` warnings).
</verification>

<success_criteria>
- `AgentToolCall` struct exists with `{id, name, input}` and Sendable + Equatable conformance.
- `AgentLoopProviderResponse.toolCalls` has type `[AgentToolCall]`.
- All three adapters build and decode/encode `AgentToolCall` natively.
- Each adapter has its own test file with at least decode + encode + round-trip tests.
- DeepSeek and Kimi adapters contain NO JSON-in-text fallback for tool calls. (Grep confirms.)
- Build green, tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/43-extract-agent-policy-data-to-versioned-resources-and-achieve-provider-parity-for-tool-use-across-anthropic-deepseek-and-kimi/43-03-SUMMARY.md` documenting: (a) the exact LOC delta in each adapter file, (b) any pre-existing JSON-in-text fallback that was deleted (if any), (c) the fixture set used in the round-trip tests, (d) any provider quirks discovered during smoke testing (e.g. Kimi `tool_call_id` format differences, Anthropic `tool_use` content-block ordering edges).
</output>
