---
phase: 39-move-wiki-to-cellar-memory
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - Package.swift
  - Sources/cellar/wiki/
autonomous: false
requirements: []

must_haves:
  truths:
    - Package.swift no longer references .copy("wiki") — the cellar SPM target has no wiki bundle resource
    - Sources/cellar/wiki/ directory is deleted from the repo
    - The 11 wiki markdown files have been pushed to lasermaze/cellar-memory/wiki/ on the main branch
    - swift build succeeds with the wiki bundle removed
    - A fresh run of the cellar CLI populates ~/.cellar/wiki/index.md from GitHub on first WikiService.fetchContext call
  artifacts:
    - Updated Package.swift without .copy("wiki")
    - Deletion of Sources/cellar/wiki/ in the working tree
  key_links:
    - cellar-memory repo now holds wiki/ alongside entries/
---

<objective>
Complete the migration by removing the now-unused SPM wiki bundle and ensuring the wiki seed content lives in the cellar-memory GitHub repo where the Worker can write and GitHub raw URLs can serve it.

Purpose: Phases 39-01 through 39-03 make the cellar CLI read and write wiki content through the cellar-memory repo. This plan is the cleanup — delete the old bundled copy so there is a single source of truth, and make the seed pages available on the remote so fresh installs have something to read.
Output: A smaller cellar SPM target, a deleted Sources/cellar/wiki/ directory, and a pushed wiki/ directory in cellar-memory.

This plan contains a human checkpoint because pushing to the external cellar-memory repository is an out-of-tree git operation.
</objective>

<execution_context>
@/Users/peter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-move-wiki-to-cellar-memory-github-repo-shared-growing-artifact-via-cloudflare-worker/39-CONTEXT.md
@.planning/phases/39-move-wiki-to-cellar-memory-github-repo-shared-growing-artifact-via-cloudflare-worker/39-RESEARCH.md
@Package.swift
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Push Sources/cellar/wiki/ content to cellar-memory repo (manual)</name>
  <files>Sources/cellar/wiki/ (read-only; pushed to external repo)</files>
  <action>
BEFORE running Task 2 (which deletes Sources/cellar/wiki/ from the cellar repo), the contents of Sources/cellar/wiki/ MUST exist at lasermaze/cellar-memory/wiki/ on the main branch, otherwise fresh installs will have nothing to fetch.

Manual steps for the operator:

1. Clone or pull the cellar-memory repo in a scratch directory:
   ```
   git clone https://github.com/lasermaze/cellar-memory.git /tmp/cellar-memory
   cd /tmp/cellar-memory
   ```

2. Copy the seed wiki tree verbatim:
   ```
   mkdir -p wiki
   cp -R /Users/peter/Documents/Cellar/Sources/cellar/wiki/. wiki/
   ```
   This should produce wiki/SCHEMA.md, wiki/index.md, wiki/log.md, wiki/engines/, wiki/symptoms/, wiki/environments/, wiki/games/.

3. Commit and push:
   ```
   git add wiki
   git commit -m "wiki: seed from cellar Sources/cellar/wiki (phase 39 migration)"
   git push origin main
   ```

4. Verify the seed is live by fetching a raw URL:
   ```
   curl -sf https://raw.githubusercontent.com/lasermaze/cellar-memory/main/wiki/index.md | head
   ```
   A successful fetch (200, non-empty body) confirms Plan 01's read path will work on fresh installs.

5. Confirm this checkpoint complete in the execute-phase chat before Task 2 runs.

DO NOT proceed to Task 2 until the raw URL check above returns the index.md content.
  </action>
  <verify>
    <automated>curl -sf https://raw.githubusercontent.com/lasermaze/cellar-memory/main/wiki/index.md -o /dev/null && echo ok || exit 1</automated>
  </verify>
  <done>curl on the raw URL of wiki/index.md in cellar-memory/main returns 200 with the index content; operator has confirmed in chat.</done>
</task>

<task type="auto">
  <name>Task 2: Remove .copy("wiki") from Package.swift and delete Sources/cellar/wiki/</name>
  <files>Package.swift, Sources/cellar/wiki/</files>
  <action>
1. Edit Package.swift — find the `.copy("wiki")` line inside the cellar target's resources array and remove it. If the resources array becomes empty, remove the `resources:` parameter entirely (do not leave an empty array that changes the SPM target shape more than needed; match Swift style used elsewhere in the file). Do not touch any other resource declarations.

2. Delete the entire Sources/cellar/wiki/ directory. Use `rm -rf Sources/cellar/wiki` (all 11 markdown files plus subdirectories).

3. Run `swift build` — it must succeed. If it fails with a "no such resource" error, grep for any lingering `Bundle.module.url(forResource: "wiki"` / `forResource: "SCHEMA"` / etc. in the Swift sources and remove or rewrite them. Plans 01 and 02 should already have eliminated these, but this is a safety net.

4. Run `swift test` — all existing tests must continue to pass. If a test touches WikiService and relies on bundled fixtures, update it to write fixtures into a temp directory under CellarPaths.wikiCacheDir instead (per RESEARCH pitfall #3). Do not add any new tests in this plan.
  </action>
  <verify>
    <automated>swift build 2>&1 | grep -E "error:" && exit 1 || swift test 2>&1 | tail -5</automated>
  </verify>
  <done>Package.swift no longer mentions "wiki"; Sources/cellar/wiki/ is gone; swift build succeeds; swift test passes with the same test count as before (or fewer only if a broken fixture test was updated, not deleted).</done>
</task>

</tasks>

<verification>
- `grep -R "copy(\"wiki\")" Package.swift` returns no matches
- `test -d Sources/cellar/wiki && echo FAIL || echo ok` prints ok
- `grep -R "Bundle.module" Sources/cellar/Core/WikiService.swift` returns no matches
- `swift build && swift test` succeeds with all tests passing
- Manual smoke test: delete `~/.cellar/wiki/`, run `cellar` on a game that triggers fetchContext, and confirm `~/.cellar/wiki/index.md` appears after the call
</verification>

<success_criteria>
Phase 39 is complete: the SPM target ships no wiki bundle, the cellar-memory repo holds the canonical wiki seed, the Swift client reads from GitHub via a local cache, and ingest writes through the Cloudflare Worker.
</success_criteria>

<output>
After completion, create `.planning/phases/39-move-wiki-to-cellar-memory-github-repo-shared-growing-artifact-via-cloudflare-worker/39-04-SUMMARY.md`
</output>
