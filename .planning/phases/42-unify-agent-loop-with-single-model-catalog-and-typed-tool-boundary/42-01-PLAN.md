---
phase: 42-unify-agent-loop-with-single-model-catalog-and-typed-tool-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/cellar/Core/ModelCatalog.swift
  - Sources/cellar/AIService.swift
  - Sources/cellar/Core/AgentLoopProvider.swift
autonomous: true
requirements: []

must_haves:
  truths:
    - "Single source of truth for model identity, pricing, and max-output limits exists in ModelCatalog.swift"
    - "Unknown model IDs throw a clear, user-visible error at session boundary (no silent (0,0) pricing fallback)"
    - "All four `if requested == \"kimi\"`-style provider routing branches in AIService.swift collapse to one catalog lookup"
    - "Pricing displayed and budget calculations match the prior values (no behavior change for known models)"
    - "Build green; smoke session with Anthropic, Deepseek, and Kimi models still launches"
  artifacts:
    - path: "Sources/cellar/Core/ModelCatalog.swift"
      provides: "ModelDescriptor struct + ModelCatalog static table + strict resolver + ModelCatalogError"
      contains: "enum ModelCatalog"
    - path: "Sources/cellar/AIService.swift"
      provides: "Single resolution site via ModelCatalog.descriptor(for:); provider-routing branches removed"
      contains: "ModelCatalog.descriptor"
  key_links:
    - from: "Sources/cellar/AIService.swift"
      to: "Sources/cellar/Core/ModelCatalog.swift"
      via: "ModelCatalog.descriptor(for: requestedID) at session boundary"
      pattern: "ModelCatalog\\.descriptor"
    - from: "Sources/cellar/Core/AgentLoopProvider.swift"
      to: "Sources/cellar/Core/ModelCatalog.swift"
      via: "Pricing/maxOutputTokens read from descriptor (injected at provider init), not from per-provider modelPricing dict"
      pattern: "ModelDescriptor|descriptor\\."
---

<objective>
Create the single model catalog and collapse all scattered model/provider routing into it. This is the foundational commit for Phase 42 because `ModelDescriptor.provider: AIProvider` is the discriminant the next plan's `AgentProvider.init` uses to choose an adapter.

Purpose: Eliminate the two parallel sources of model truth (`AgentLoopProvider.swift` per-provider `modelPricing` dicts + `AIService.fallbackModels` + four `if requested == "kimi"` branches). Replace silent `?? (0.0, 0.0)` pricing fallback with strict throw so unknown model IDs surface a clear error.

Output: `Sources/cellar/Core/ModelCatalog.swift` (new), modified `AIService.swift` (one resolution site, no provider-routing branches), modified `AgentLoopProvider.swift` (per-provider dicts deleted; pricing/limits injected from descriptor at init).
</objective>

<execution_context>
@/Users/peter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/42-unify-agent-loop-with-single-model-catalog-and-typed-tool-boundary/42-CONTEXT.md
@.planning/phases/42-unify-agent-loop-with-single-model-catalog-and-typed-tool-boundary/42-RESEARCH.md
@Sources/cellar/Models/AIModels.swift
@Sources/cellar/AIService.swift
@Sources/cellar/Core/AgentLoopProvider.swift

<interfaces>
<!-- AIProvider already exists at Sources/cellar/Models/AIModels.swift:5 — reuse, do not redefine. -->

Catalog shape (lean, per CONTEXT.md):
```swift
struct ModelDescriptor: Sendable {
    let id: String
    let provider: AIProvider          // existing enum
    let inputPricePerToken: Double
    let outputPricePerToken: Double
    let maxOutputTokens: Int
}

enum ModelCatalog {
    static let all: [ModelDescriptor] = [ /* anthropic + deepseek + kimi entries */ ]
    static func descriptor(for id: String) throws -> ModelDescriptor { /* strict, throws on unknown */ }
}

enum ModelCatalogError: Error, LocalizedError {
    case unknownModel(String)
}
```

Catalog entries to copy from existing per-provider `modelPricing` dicts in `AgentLoopProvider.swift` (lines ~43-49, 264-265, 469-470 per RESEARCH.md). Verify against current pricing while copying. `deepseek-reasoner` intentionally absent (Phase 18: no function-calling support).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ModelCatalog.swift with descriptor, table, and strict resolver</name>
  <files>Sources/cellar/Core/ModelCatalog.swift</files>
  <action>
Create new file `Sources/cellar/Core/ModelCatalog.swift`. Define:

1. `struct ModelDescriptor: Sendable` with fields exactly: `id: String`, `provider: AIProvider`, `inputPricePerToken: Double`, `outputPricePerToken: Double`, `maxOutputTokens: Int`. No displayLabel, no contextWindow, no supportsToolUse — keep lean per CONTEXT.md.

2. `enum ModelCatalog` (caseless) with `static let all: [ModelDescriptor]`. Populate by copying values verbatim from the existing per-provider `modelPricing` dictionaries inside `Sources/cellar/Core/AgentLoopProvider.swift`:
   - Anthropic entries (claude-opus-4-5, claude-sonnet-4-6, claude-haiku-3-5, plus any other current entries — read the file's anthropic dict)
   - Deepseek entries (deepseek-chat — exclude `deepseek-reasoner` per Phase 18)
   - Kimi entries (moonshot-v1-128k, moonshot-v1-32k, plus any others present)
   For `maxOutputTokens` copy the value each provider currently advertises (look at the `maxOutputTokensLimit` computed property or constants in each provider class).

3. `static func descriptor(for id: String) throws -> ModelDescriptor` that calls `all.first(where: { $0.id == id })` and throws `ModelCatalogError.unknownModel(id)` on miss. NO silent fallback to `(0.0, 0.0)`.

4. `enum ModelCatalogError: Error, LocalizedError` with `case unknownModel(String)` and `errorDescription` returning `"Unknown model '\(id)'. Check your AI model setting."`.

DO NOT modify AIService or AgentLoopProvider yet — this task is purely additive.
  </action>
  <verify>
    <automated>swift build 2>&1 | tee /tmp/p42-01-t1-build.log | tail -20 && grep -q "Build complete" /tmp/p42-01-t1-build.log</automated>
  </verify>
  <done>
File exists, compiles. `ModelCatalog.all` contains entries for all current models across the three providers. `ModelCatalog.descriptor(for: "claude-sonnet-4-6")` returns a valid descriptor; `descriptor(for: "bogus-id")` throws `ModelCatalogError.unknownModel`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Collapse AIService provider-routing branches and inject pricing into providers from catalog</name>
  <files>Sources/cellar/AIService.swift, Sources/cellar/Core/AgentLoopProvider.swift</files>
  <action>
**Part A — AIService.swift:**

Find the four scattered provider-routing branches (the research cites lines 227, 311, 439, 662, but line numbers may have drifted — search for `requested == "kimi"`, `requested == "moonshot"`, `model.contains("kimi")`, and similar string-comparison branches that switch behavior on provider).

At each session-boundary site that currently branches on the model string, replace with:

```swift
let descriptor = try ModelCatalog.descriptor(for: requestedModelID)
switch descriptor.provider {
case .anthropic: /* existing anthropic path */
case .deepseek:  /* existing deepseek path */
case .kimi:      /* existing kimi path */
}
```

Pass the `descriptor` (not the raw model string) into the provider construction so pricing/limits flow from the catalog. Where pricing is currently looked up via the provider's `pricingPerToken()`, that still works in Part B, but the descriptor is now the source.

`fallbackModels: [ModelOption]` array: replace its body with a derivation from `ModelCatalog.all` (`ModelCatalog.all.map { ModelOption(id: $0.id, ...) }`). Keep the `ModelOption` struct for the web layer (do not delete) but its data now flows from the catalog. Delete any hand-authored entries.

Catalog-resolution errors must surface to the UI via the existing `AgentEvent.error(String)` path (or equivalent). Do NOT swallow with `try?` — the user must see "Unknown model 'X'..." if their config is bad.

**Part B — AgentLoopProvider.swift (transitional):**

Delete the per-provider `modelPricing: [String: (Double, Double)]` static/instance dictionaries from the three provider classes. Update each provider's initializer to accept and store the resolved `ModelDescriptor` (or the pricing tuple + maxOutputTokens unpacked from it). `pricingPerToken()` now returns `(descriptor.inputPricePerToken, descriptor.outputPricePerToken)`. `maxOutputTokensLimit` returns `descriptor.maxOutputTokens`.

This keeps the `AgentLoopProvider` protocol intact (its full deletion is Plan 02) but removes the duplicated pricing data. Build must stay green.

After this task, grep `Sources/cellar/AIService.swift` for `requested == "kimi"` or `model.contains("kimi")` — should return zero matches. Grep for `modelPricing` in `AgentLoopProvider.swift` — should return zero matches.
  </action>
  <verify>
    <automated>swift build 2>&1 | tee /tmp/p42-01-t2-build.log | tail -30 && grep -q "Build complete" /tmp/p42-01-t2-build.log && ! grep -E 'requested == "(kimi|moonshot)"|modelPricing\s*[:=]' Sources/cellar/AIService.swift Sources/cellar/Core/AgentLoopProvider.swift</automated>
  </verify>
  <done>
Build green. Zero `requested == "kimi"` style branches remain in AIService.swift. Zero `modelPricing` dictionaries remain in AgentLoopProvider.swift. Each provider class accepts a `ModelDescriptor` (or its unpacked fields) at init. Manual smoke: starting a session with a known model still works; setting an unknown model in config surfaces "Unknown model 'X'" to the user.
  </done>
</task>

</tasks>

<verification>
- `swift build` succeeds with no errors.
- `Sources/cellar/Core/ModelCatalog.swift` exists with `ModelDescriptor`, `ModelCatalog.all`, `ModelCatalog.descriptor(for:)`, `ModelCatalogError`.
- Zero remaining `if requested == "kimi"` / `requested == "moonshot"` / `model.contains("kimi")` branches in `AIService.swift`.
- Zero remaining `modelPricing` dictionaries in `AgentLoopProvider.swift`.
- Manual smoke: launch a session with each of (a) a Claude model, (b) deepseek-chat, (c) a moonshot-v1 model. All three start the agent loop and tools dispatch.
- Manual smoke: set `aiModel` to a fake string in CellarConfig, launch — error message reaches the UI rather than crashing or silently falling back.
</verification>

<success_criteria>
- Single source of truth for model id, provider, pricing, and maxOutputTokens lives in `ModelCatalog.all`.
- Strict resolution at session boundary; no silent `(0.0, 0.0)` pricing fallback.
- All four scattered provider-routing branches collapsed to one catalog lookup.
- `AgentLoopProvider` protocol still exists but its three implementations no longer carry duplicated pricing data — Plan 02 will delete the protocol entirely.
</success_criteria>

<output>
After completion, create `.planning/phases/42-unify-agent-loop-with-single-model-catalog-and-typed-tool-boundary/42-01-SUMMARY.md` describing:
- ModelDescriptor field set actually used
- Number of catalog entries by provider (anthropic/deepseek/kimi)
- Any pricing values that needed verification or differed from prior dicts
- Whether `fallbackModels` was kept as thin view or removed
- File line counts: AgentLoopProvider.swift before/after pricing-dict removal
</output>
