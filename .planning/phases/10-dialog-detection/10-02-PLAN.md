---
phase: 10-dialog-detection
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - Sources/cellar/Core/AIService.swift
autonomous: true
requirements: [DIAG-03]

must_haves:
  truths:
    - "System prompt includes dialog detection methodology section with multi-signal heuristic guidance"
    - "Agent knows to call list_windows after launch_game when game exits quickly or msgbox data is present"
    - "Agent knows to probe list_windows early in session to test Screen Recording permission"
    - "Agent knows to ask user once about Screen Recording permission if denied, then continue with trace:msgbox only"
    - "Agent can distinguish dialog-stuck from crash from running-normally using combined signals"
  artifacts:
    - path: "Sources/cellar/Core/AIService.swift"
      provides: "Dialog detection methodology in system prompt"
      contains: "Dialog Detection"
  key_links:
    - from: "AIService.swift system prompt"
      to: "Agent reasoning about dialog signals"
      via: "Heuristic table and common pattern guidance"
      pattern: "dialog_detected|msgbox|list_windows"
---

<objective>
Add dialog detection methodology to the agent system prompt with hybrid signal heuristics, common dialog patterns, and permission handling guidance.

Purpose: The agent needs to reason about combined signals from trace:msgbox parsing and list_windows to determine if a game is stuck on a dialog vs running normally vs crashed. This is purely agent-level reasoning guided by the system prompt — no hardcoded hybrid logic in tool code (per CONTEXT.md decision).

Output: Updated AIService.swift system prompt with dialog detection section.
</objective>

<execution_context>
@/Users/peter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dialog-detection/10-CONTEXT.md
@.planning/phases/10-dialog-detection/10-RESEARCH.md
@.planning/phases/10-dialog-detection/10-01-SUMMARY.md
@Sources/cellar/Core/AIService.swift

<interfaces>
<!-- System prompt structure in AIService.swift -->

Current section order (from AIService.swift):
1. Three-Phase Workflow: Research -> Diagnose -> Adapt (line 513)
2. Engine-Aware Methodology (line 538)
3. macOS + Wine Domain Knowledge (line 567)
4. Available Tools (19 total) (line 579)
5. Constraints (line 586)

The Dialog Detection section should go BETWEEN "Engine-Aware Methodology" and "macOS + Wine Domain Knowledge" — it is a detection methodology that complements engine detection and informs the diagnostic workflow.

From CONTEXT.md locked decisions:
- Agent combines signals itself — no hardcoded hybrid logic in tool code
- System prompt includes multi-signal heuristic guidance
- System prompt includes common dialog pattern guidance
- Proactive permission probe: system prompt tells agent to call list_windows once early in session
- If denied, agent tells user once via ask_user about Screen Recording permission
- Never ask user about permission more than once per session
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dialog detection methodology section to system prompt</name>
  <files>Sources/cellar/Core/AIService.swift</files>
  <action>
In AIService.swift, locate the system prompt string. Find the "## Engine-Aware Methodology" section (line 538) which ends with the "Success Database Cross-Reference" subsection (around line 565). Insert a new section AFTER Engine-Aware Methodology and BEFORE "## macOS + Wine Domain Knowledge" (line 567).

Add the following section (adapt formatting to match existing prompt style — plain text with markdown headers, no code blocks in the prompt string):

```
## Dialog Detection

After calling launch_game or trace_launch, check the `dialogs` array in the result. This contains MessageBox text captured from Wine's +msgbox trace channel.

### Permission Probe (once per session)
Call list_windows once early in the session (after inspect_game, before first launch) to test Screen Recording permission:
- If screen_recording_permission is true: you have full window data (titles + sizes) for the rest of the session
- If screen_recording_permission is false: tell the user ONCE via ask_user: "For best dialog detection, grant Screen Recording permission to Terminal in System Settings > Privacy & Security > Screen Recording." Then continue with trace:msgbox as sole signal. Do NOT ask about permission again.

### Multi-Signal Heuristics
Combine launch_game results with list_windows to determine game state:

| Exit Behavior | dialogs Array | list_windows | Diagnosis |
|---------------|---------------|--------------|-----------|
| Quick exit (< 5s) | Has entries | N/A | Dialog blocked then dismissed/crashed — read dialog text for cause |
| Quick exit (< 5s) | Empty | N/A | Crash or missing dependency — check stderr_tail and detected_errors |
| Still running | Has entries | Small window (<640x480) | Dialog waiting for user input — game is stuck |
| Still running | Empty | Small window (<640x480) | Possible dialog without msgbox (custom window) — investigate |
| Still running | Empty | Large window (>=640x480) | Game running normally |
| Still running | N/A | No windows found | Game may be initializing or running headless — wait and retry list_windows |

Call list_windows after launch_game when: game exits quickly, dialogs array has entries, or you need to verify the game is actually running. Do NOT call list_windows after every launch — only when there is reason to investigate.

### Common Dialog Patterns
When dialogs are detected, use the message text to determine the fix:

- **Renderer/video mode selection** ("Select Rendering Device", "Choose Display", "Video Options"): Pre-configuration should have prevented this. Apply engine pre-config (cnc-ddraw for DirectDraw, renderer INI for Unreal) and relaunch.
- **Missing file/DLL** ("could not find", "failed to load", "missing"): Check which file is referenced, use place_dll or install_winetricks to provide it.
- **Runtime error** ("abnormal program termination", "Runtime Error"): Usually a crash, not a blocking dialog. Check stderr for more details.
- **Registration/serial** ("enter your", "registration", "serial number", "CD key"): Informational — tell user via ask_user, these usually have a Cancel/Skip button.
- **DirectX/driver version** ("requires DirectX", "Direct3D not available"): Configure WINEDLLOVERRIDES or install directx9 via winetricks.

### Connecting to Engine Pre-Configuration
If a dialog is detected that pre-configuration should have prevented (renderer selection for a known DirectDraw engine, for example):
1. The engine detection in inspect_game may have missed the game, OR
2. The pre-configuration was incomplete
Apply the fix now, save it to the recipe, and note the gap for save_success.
```

Also update the Phase 3 Adapt workflow (around line 531-536) to mention checking dialogs after launch. After the existing step "2. Call launch_game for a real launch attempt", add:
"2b. Check the dialogs array in the launch result — if dialogs are present, diagnose using Dialog Detection methodology below before asking the user"

This mirrors the existing "2b. Check the engine and graphics_api fields" pattern in Phase 1 Research (line 520).
  </action>
  <verify>
    <automated>cd /Users/peter/Documents/Cellar && swift build 2>&1 | tail -5</automated>
  </verify>
  <done>System prompt contains Dialog Detection section with permission probe guidance, multi-signal heuristic table, common dialog patterns, and engine pre-config connection. Phase 3 Adapt workflow references dialog checking after launch.</done>
</task>

</tasks>

<verification>
1. `swift build` compiles without errors
2. Grep for "Dialog Detection" in AIService.swift confirms new section exists
3. Grep for "Permission Probe" in AIService.swift confirms permission handling guidance
4. Grep for "Multi-Signal Heuristics" in AIService.swift confirms heuristic table
5. Grep for "Common Dialog Patterns" in AIService.swift confirms pattern guidance
6. Grep for "dialogs array" in AIService.swift confirms Phase 3 workflow update
7. Section ordering: Dialog Detection appears after Engine-Aware Methodology and before macOS + Wine Domain Knowledge
</verification>

<success_criteria>
- System prompt includes complete dialog detection methodology between Engine-Aware and Domain Knowledge sections
- Agent has clear heuristic table for combining trace:msgbox + list_windows signals
- Permission probe guidance tells agent to test once early and ask user once if denied
- Common dialog patterns give agent actionable responses for each dialog type
- Phase 3 workflow updated to check dialogs after launch
- Project builds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/10-dialog-detection/10-02-SUMMARY.md`
</output>
