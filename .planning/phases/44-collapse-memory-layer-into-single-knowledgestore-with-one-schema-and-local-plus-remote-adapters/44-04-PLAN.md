---
phase: 44-collapse-memory-layer-into-single-knowledgestore-with-one-schema-and-local-plus-remote-adapters
plan: 04
type: execute
wave: 3
depends_on:
  - 44-01
  - 44-03
files_modified:
  - Sources/cellar/Core/AIService.swift
  - Sources/cellar/Core/CollectiveMemoryService.swift
  - Sources/cellar/Core/CollectiveMemoryWriteService.swift
  - Sources/cellar/Core/WikiService.swift
  - Sources/cellar/Core/WikiIngestService.swift
  - Sources/cellar/Core/Tools/ResearchTools.swift
  - Sources/cellar/Core/Tools/SaveTools.swift
  - Sources/cellar/Core/AgentTools.swift
  - Sources/cellar/Core/Tools/ConfigTools.swift
  - Tests/cellarTests/KnowledgeStoreIntegrationTests.swift
autonomous: true
requirements:
  - KS-WIRE-AISERVICE
  - KS-WIRE-AGENT-TOOLS
  - KS-LEGACY-WRAPPERS

must_haves:
  truths:
    - "AIService.runAgentLoop calls KnowledgeStoreContainer.shared.fetchContext for the unified context block — replaces today's WikiService.fetchContext call (line 779)"
    - "AIService success path calls KnowledgeStoreContainer.shared.write(.sessionLog(...)) and .write(.config(...)) instead of WikiService.postSessionLog and CollectiveMemoryWriteService.push"
    - "AIService failure path calls KnowledgeStoreContainer.shared.write(.sessionLog(...)) for the failure log — replaces WikiService.postFailureSessionLog"
    - "AgentTools query_wiki tool routes through KnowledgeStoreContainer.shared — replaces WikiService.search direct call"
    - "WikiIngestService.ingest calls KnowledgeStoreContainer.shared.write(.gamePage(...)) — replaces direct postWikiAppend call"
    - "CollectiveMemoryService, CollectiveMemoryWriteService, WikiService, WikiIngestService are now thin wrappers that delegate to KnowledgeStoreContainer.shared"
    - "The hardcoded AgentTools.agentValidWinetricksVerbs Set is REMOVED; all former call sites read PolicyResources.shared.winetricksVerbAllowlist"
    - "All 186+ existing tests still pass; new integration test confirms the call sites route through the store"
    - "Build is green with no deprecation warnings related to the new wiring"
  artifacts:
    - path: "Sources/cellar/Core/AIService.swift"
      provides: "Five memory call sites rewired (lines 779, 854, 856, 903, 983 per RESEARCH.md)"
      contains: "KnowledgeStoreContainer.shared"
    - path: "Sources/cellar/Core/CollectiveMemoryService.swift"
      provides: "Thin wrapper delegating to KnowledgeStoreContainer.shared.fetchContext"
    - path: "Sources/cellar/Core/CollectiveMemoryWriteService.swift"
      provides: "Thin wrapper delegating to KnowledgeStoreContainer.shared.write(.config)"
    - path: "Sources/cellar/Core/WikiService.swift"
      provides: "Thin wrapper for fetchContext/search/postSessionLog/postFailureSessionLog/ingest"
    - path: "Sources/cellar/Core/WikiIngestService.swift"
      provides: "Thin wrapper that builds a GamePageEntry and writes via the store"
    - path: "Sources/cellar/Core/Tools/ResearchTools.swift"
      provides: "queryWiki tool routes through KnowledgeStoreContainer.shared"
    - path: "Sources/cellar/Core/AgentTools.swift"
      provides: "agentValidWinetricksVerbs Set deleted; remaining usages updated to PolicyResources.shared.winetricksVerbAllowlist"
    - path: "Tests/cellarTests/KnowledgeStoreIntegrationTests.swift"
      provides: "Integration test that swaps KnowledgeStoreContainer.shared with a stub and confirms call sites route through it"
  key_links:
    - from: "Sources/cellar/Core/AIService.swift runAgentLoop"
      to: "KnowledgeStoreContainer.shared.fetchContext"
      via: "direct call replacing WikiService.fetchContext"
      pattern: "KnowledgeStoreContainer\\.shared\\.fetchContext"
    - from: "Sources/cellar/Core/AIService.swift success and failure paths"
      to: "KnowledgeStoreContainer.shared.write"
      via: "awaited calls replacing WikiService.postSessionLog and CollectiveMemoryWriteService.push"
      pattern: "KnowledgeStoreContainer\\.shared\\.write"
    - from: "Application startup (AIService.runAgentLoop entry or app init)"
      to: "KnowledgeStoreContainer.shared = KnowledgeStoreRemote()"
      via: "one-time assignment; idempotent guard"
      pattern: "KnowledgeStoreContainer\\.shared ="
---

<objective>
Cut over all five AIService memory call sites + AgentTools query_wiki + WikiIngestService to KnowledgeStoreContainer.shared. Convert the four legacy services into thin wrappers so any straggler caller still compiles. Delete the hardcoded `agentValidWinetricksVerbs` Set now that PolicyResources owns it.

Purpose: This is the cutover plan. After this lands, the unified store is the live data path. The four legacy types remain as compatibility shims and will be deleted in a follow-up phase. The fenced-section merge (Plan 02) becomes effective for `cellar wiki ingest` because WikiIngestService now writes via `/api/knowledge/write`.

Output: One unified read+write path through KnowledgeStore. Phase goal met: existing services thin wrappers, no allowlist duplication, Worker generalized.
</objective>

<execution_context>
@/Users/peter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-collapse-memory-layer-into-single-knowledgestore-with-one-schema-and-local-plus-remote-adapters/44-CONTEXT.md
@.planning/phases/44-collapse-memory-layer-into-single-knowledgestore-with-one-schema-and-local-plus-remote-adapters/44-RESEARCH.md
@.planning/phases/44-collapse-memory-layer-into-single-knowledgestore-with-one-schema-and-local-plus-remote-adapters/44-01-SUMMARY.md
@.planning/phases/44-collapse-memory-layer-into-single-knowledgestore-with-one-schema-and-local-plus-remote-adapters/44-03-SUMMARY.md

# Files to modify (read before editing)
@Sources/cellar/Core/AIService.swift
@Sources/cellar/Core/CollectiveMemoryService.swift
@Sources/cellar/Core/CollectiveMemoryWriteService.swift
@Sources/cellar/Core/WikiService.swift
@Sources/cellar/Core/WikiIngestService.swift
@Sources/cellar/Core/Tools/ResearchTools.swift
@Sources/cellar/Core/Tools/SaveTools.swift
@Sources/cellar/Core/AgentTools.swift
@Sources/cellar/Core/Tools/ConfigTools.swift

# Plan outputs already in place
@Sources/cellar/Core/KnowledgeStore.swift
@Sources/cellar/Core/KnowledgeEntry.swift
@Sources/cellar/Core/KnowledgeStoreRemote.swift
@Sources/cellar/Core/KnowledgeStoreLocal.swift

<interfaces>
Exact AIService call sites to rewire (audited 2026-05-03 from RESEARCH.md).

Sources/cellar/Core/AIService.swift line 779 — current:
    if let wikiContext = await WikiService.fetchContext(engine: entry.name) {
        contextParts.append(wikiContext)
    }

Sources/cellar/Core/AIService.swift lines 853-865 (success path) — current:
    if let record = SuccessDatabase.load(gameId: gameId) {
        await WikiService.ingest(record: record)
        await WikiService.postSessionLog(
            record: record, outcome: .success,
            duration: Date().timeIntervalSince(sessionStartTime),
            wineURL: wineURL, midSessionNotes: tools.draftBuffer.notes
        )
        tools.draftBuffer.clearDraft()
    }

Sources/cellar/Core/AIService.swift line ~903 (failure path) — current:
    await WikiService.postFailureSessionLog(
        gameId: gameId, gameName: entry.name,
        narrative: trimmedFinal, actionsAttempted: actions,
        launchCount: tools.launchCount,
        duration: Date().timeIntervalSince(sessionStartTime),
        wineURL: wineURL, stopReason: stopReasonStr,
        midSessionNotes: tools.draftBuffer.notes
    )

Sources/cellar/Core/AIService.swift line ~983 (config push) — current:
    await CollectiveMemoryWriteService.push(record: record, gameName: gameName, wineURL: wineURL)

Sources/cellar/Core/Tools/ResearchTools.swift queryWiki — current:
    let result = await WikiService.search(query: query)
    return result   // never nil, always String

Sources/cellar/Core/AgentTools.swift line 209 — TO BE DELETED:
    extension AgentTools {
        static let agentValidWinetricksVerbs: Set<String> = [ ... ]
    }

Plan 01 / Plan 03 outputs (now available):
    protocol KnowledgeStore: Sendable {
        func fetchContext(for: String, environment: EnvironmentFingerprint) async -> String?
        func write(_ entry: KnowledgeEntry) async
        func list(filter: KnowledgeListFilter) async -> [KnowledgeEntryMeta]
    }
    enum KnowledgeStoreContainer { static var shared: KnowledgeStore = NoOpKnowledgeStore() }
    struct KnowledgeStoreRemote: KnowledgeStore { init(...) }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire startup; rewire AIService five call sites; delete agentValidWinetricksVerbs literal</name>
  <files>Sources/cellar/Core/AIService.swift, Sources/cellar/Core/AgentTools.swift, Sources/cellar/Core/Tools/ConfigTools.swift, Sources/cellar/Core/CollectiveMemoryService.swift, Tests/cellarTests/KnowledgeStoreIntegrationTests.swift</files>
  <behavior>
    - Test 1: Set KnowledgeStoreContainer.shared = StubKnowledgeStore() and call a small AIService entry point that exercises fetchContext; assert the stub's fetchContext recorded one call with the expected gameName and environment fingerprint
    - Test 2: Same pattern for the success path — assert .write(.config(...)) and .write(.sessionLog(...)) are both invoked on the stub
    - Test 3: Same for the failure path — assert .write(.sessionLog(...)) is invoked with the failure body
    - Test 4: After this task, `grep -n "agentValidWinetricksVerbs" Sources/` returns 0 matches in the literal definition; only references to PolicyResources.shared.winetricksVerbAllowlist remain
    - Test 5: Existing 186 tests still pass
  </behavior>
  <action>
    Step 1: Register KnowledgeStoreContainer.shared at startup.

    Find the AIService initialization point. If AIService has no static init, add the registration at the top of `runAgentLoop` (before any await):
        if KnowledgeStoreContainer.shared is NoOpKnowledgeStore {
            KnowledgeStoreContainer.shared = KnowledgeStoreRemote()
        }
    Better: register it once at the application's earliest entry point (look for `Cellar.swift` or `ServeCommand` startup) consistent with how PolicyResources.shared is initialized today. The `is NoOpKnowledgeStore` check requires the NoOp type to be visible — promote it from `private` to `internal` in `KnowledgeStore.swift` (small Plan 01 retroactive change is acceptable; document in SUMMARY).

    Step 2: Rewire AIService line 779 (read path).

    Replace the WikiService.fetchContext call with:
        let env = EnvironmentFingerprint.current(
            wineVersion: detectWineVersion(wineURL: wineURL) ?? "",
            wineFlavor: detectWineFlavor(wineURL: wineURL)
        )
        if let knowledgeContext = await KnowledgeStoreContainer.shared.fetchContext(for: entry.name, environment: env) {
            contextParts.append(knowledgeContext)
        }
    The new context block now folds in config + game page + sessions in one call. This restores the config-entry capability (RESEARCH.md open question #1: restore).

    Step 3: Rewire AIService success path (lines 853-865 + line 983).

    Replace `WikiService.ingest + WikiService.postSessionLog + CollectiveMemoryWriteService.push` with three KnowledgeStoreContainer.shared.write calls:

        if let record = SuccessDatabase.load(gameId: gameId) {
            // Game page (was WikiService.ingest)
            if let gamePage = WikiService.buildIngestedGamePage(record: record) {
                await KnowledgeStoreContainer.shared.write(.gamePage(gamePage))
            }
            // Session log (was WikiService.postSessionLog)
            let sessionEntry = SessionLogEntry(
                path: WikiService.sessionLogFilename(record: record),
                body: WikiService.formatSuccessSessionBody(
                    record: record,
                    duration: Date().timeIntervalSince(sessionStartTime),
                    wineURL: wineURL,
                    midSessionNotes: tools.draftBuffer.notes
                ),
                commitMessage: "session: success for \(record.gameName)"
            )
            await KnowledgeStoreContainer.shared.write(.sessionLog(sessionEntry))
            tools.draftBuffer.clearDraft()
            // Config (was CollectiveMemoryWriteService.push)
            if let cfgEntry = CollectiveMemoryWriteService.buildConfigEntry(record: record, gameName: gameName, wineURL: wineURL) {
                await KnowledgeStoreContainer.shared.write(.config(cfgEntry))
            }
        }

    Helpers `buildIngestedGamePage`, `sessionLogFilename`, `formatSuccessSessionBody`, `buildConfigEntry` are extracted from existing private logic in the legacy services (today they POST directly; we expose body construction so AIService builds typed entries and hands them to the store). Mark them `internal static` — wider than `private`, narrower than `public`. They become wrapper-internal API; the future deletion phase removes them entirely.

    Step 4: Rewire AIService failure path (line ~903).

    Replace `WikiService.postFailureSessionLog(...)` with:
        let failureEntry = SessionLogEntry(
            path: WikiService.failureSessionLogFilename(gameId: gameId),
            body: WikiService.formatFailureSessionBody(
                gameId: gameId, gameName: entry.name, narrative: trimmedFinal,
                actionsAttempted: actions, launchCount: tools.launchCount,
                duration: Date().timeIntervalSince(sessionStartTime),
                wineURL: wineURL, stopReason: stopReasonStr,
                midSessionNotes: tools.draftBuffer.notes
            ),
            commitMessage: "session: failure for \(entry.name)"
        )
        await KnowledgeStoreContainer.shared.write(.sessionLog(failureEntry))

    Step 5: Delete agentValidWinetricksVerbs literal.

    In `Sources/cellar/Core/AgentTools.swift` line 209, REMOVE the entire `static let agentValidWinetricksVerbs: Set<String> = [ ... ]` declaration. PolicyResources.shared.winetricksVerbAllowlist provides the same data (Plan 01).

    Update the three former call sites:
    - `Sources/cellar/Core/AgentTools.swift` (other usages within the same file): change `AgentTools.agentValidWinetricksVerbs` → `PolicyResources.shared.winetricksVerbAllowlist`. Use grep to find them.
    - `Sources/cellar/Core/Tools/ConfigTools.swift`: same substitution.
    - `Sources/cellar/Core/CollectiveMemoryService.swift` (sanitizeEntry — kept as part of the wrapper Task 2 builds): same substitution.

    Run `grep -rn "agentValidWinetricksVerbs" Sources/` after the edit — must return ZERO matches.

    Step 6: Add integration tests.

    Create `Tests/cellarTests/KnowledgeStoreIntegrationTests.swift`:
    - Define `actor StubKnowledgeStore: KnowledgeStore` that records calls (fetchContextCalls, writeCalls).
    - Tests swap KnowledgeStoreContainer.shared = stub (note: ordering — restore original after each test using a teardown block), invoke a public seam in AIService that triggers fetchContext/write, and assert the recorded calls.

    Finding a public seam: AIService.runAgentLoop is heavy. Prefer extracting a tiny `internal` test helper:
        internal func fetchKnowledgeContext(gameName: String, wineURL: URL) async -> String? {
            let env = EnvironmentFingerprint.current(...)
            return await KnowledgeStoreContainer.shared.fetchContext(for: gameName, environment: env)
        }
    Plan 04 is a refactor; small extractions to enable testability are in scope.

    Do NOT touch the legacy services in this task — Task 2 makes them thin wrappers. The Step 3/4 references to `WikiService.buildIngestedGamePage`, etc., assume Task 2 will land in the same plan; in this Task 1, ADD those helpers to WikiService/CollectiveMemoryWriteService as `internal static` shims (small additions, no behavior change), so this Task 1 compiles standalone. Task 2 then replaces the rest of those files' bodies.
  </action>
  <verify>
    <automated>cd /Users/peter/Documents/Cellar && swift build 2>&1 | tail -30 && swift test --filter "KnowledgeStoreIntegration" 2>&1 | tail -30 && grep -rn "agentValidWinetricksVerbs" Sources/ Tests/ | head</automated>
  </verify>
  <done>AIService five call sites rewired through KnowledgeStoreContainer.shared; agentValidWinetricksVerbs deleted (zero grep matches in Sources/); integration tests pass; existing 186 tests still pass; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert legacy services to thin wrappers; rewire AgentTools query_wiki + WikiIngestService</name>
  <files>Sources/cellar/Core/CollectiveMemoryService.swift, Sources/cellar/Core/CollectiveMemoryWriteService.swift, Sources/cellar/Core/WikiService.swift, Sources/cellar/Core/WikiIngestService.swift, Sources/cellar/Core/Tools/ResearchTools.swift, Sources/cellar/Core/Tools/SaveTools.swift</files>
  <behavior>
    - Test 1: Calling `CollectiveMemoryService.fetchBestEntry(for:wineURL:)` invokes KnowledgeStoreContainer.shared.fetchContext (verified via stub)
    - Test 2: Calling `CollectiveMemoryWriteService.push(record:gameName:wineURL:)` invokes KnowledgeStoreContainer.shared.write(.config(...))
    - Test 3: Calling `WikiService.fetchContext(engine:)` invokes KnowledgeStoreContainer.shared.fetchContext
    - Test 4: Calling `WikiService.search(query:)` invokes KnowledgeStoreContainer.shared.list (or fetchContext for the recent-sessions augment); returns non-nil String (preserve API contract per RESEARCH.md pitfall #7)
    - Test 5: Calling `WikiIngestService.ingest(gameName:)` invokes KnowledgeStoreContainer.shared.write(.gamePage(...)) — now produces a fenced-section write because Worker (Plan 02) applies fenced merge for games/ paths
    - Test 6: AgentTools query_wiki tool result includes recent session info via the unified store, NOT a direct WikiService.search call
    - Test 7: Build green, no callers of the legacy services see any signature changes
  </behavior>
  <action>
    Step 1: Convert `CollectiveMemoryService.swift` to thin wrapper.

    Keep the public method signature identical — `static func fetchBestEntry(for gameName: String, wineURL: URL) async -> String?` — but replace the body with a delegate to the store:
        let env = EnvironmentFingerprint.current(
            wineVersion: detectWineVersion(wineURL: wineURL) ?? "",
            wineFlavor: detectWineFlavor(wineURL: wineURL)
        )
        return await KnowledgeStoreContainer.shared.fetchContext(for: gameName, environment: env)

    PRESERVE the existing private helpers `detectWineVersion`, `detectWineFlavor`, `slugify`, `sanitizeEntry`, and `formatMemoryContext` — KnowledgeStoreRemote (Plan 03) may import some of them via cross-file calls. Mark anything used externally as `internal`. Delete only the network/cache logic that the wrapper no longer needs (loadFromCache, isCacheFresh, fuzzy-match — KnowledgeStore handles these).

    Step 2: Convert `CollectiveMemoryWriteService.swift` to thin wrapper.

    Add the helper `buildConfigEntry(record:gameName:wineURL:) -> CollectiveMemoryEntry?` as `internal static` (Task 1 already references it). Then make `push(record:gameName:wineURL:)` a delegating wrapper:
        guard let entry = buildConfigEntry(record: record, gameName: gameName, wineURL: wineURL) else { return }
        await KnowledgeStoreContainer.shared.write(.config(entry))

    Step 3: Convert `WikiService.swift` to thin wrapper.

    Make these public methods delegate to KnowledgeStoreContainer.shared:
    - `fetchContext(engine:symptoms:maxPages:)` — call `KnowledgeStoreContainer.shared.fetchContext(for:environment:)` with a synthetic environment fingerprint (use `EnvironmentFingerprint.current(...)`); for backward compat, accept the engine/symptoms params but pass them only via gameName/slug derivation.
    - `search(query:maxResults:)` — call `KnowledgeStoreContainer.shared.list(filter:)` and format results; ALWAYS return non-nil String per RESEARCH.md pitfall #7. If list returns empty, return a human-readable "No results for {query}." string.
    - `postSessionLog(record:outcome:duration:wineURL:midSessionNotes:)` — build a `SessionLogEntry` and call `KnowledgeStoreContainer.shared.write(.sessionLog(entry))`.
    - `postFailureSessionLog(gameId:gameName:narrative:...)` — same, building a failure-shaped SessionLogEntry.
    - `ingest(record:)` — replaced by KnowledgeStoreContainer.shared.write(.gamePage(...)) — accepts a SuccessRecord, builds a GamePageEntry via `buildIngestedGamePage`, writes through the store.

    Add (or keep) as `internal static` helpers:
    - `buildIngestedGamePage(record:) -> GamePageEntry?`
    - `sessionLogFilename(record:) -> String` and `failureSessionLogFilename(gameId:) -> String`
    - `formatSuccessSessionBody(...) -> String` and `formatFailureSessionBody(...) -> String`

    Delete the old direct postWikiAppend network calls inside WikiService — KnowledgeStoreContainer.shared.write owns the network now.

    Step 4: Convert `WikiIngestService.swift` to thin wrapper.

    The CLI `cellar wiki ingest` calls `WikiIngestService.ingest(gameName:) async -> Bool`. Keep the signature. Body:
    1. Existing TTL check (read raw GitHub for `Last updated:` timestamp on `wiki/games/{slug}.md`) — KEEP this; the store doesn't expose freshness checks.
    2. Existing source aggregation (CompatibilityReport + WineHQ + PCGamingWiki + Lutris) — KEEP this; this is upstream-data assembly, not memory layer.
    3. Format via existing `formatGamePage(...)` — KEEP.
    4. REPLACE the final `WikiService.postWikiAppend(...)` call with:
        let entry = GamePageEntry(slug: slug, autoContent: formattedBody, commitMessage: "ingest: \(gameName)")
        await KnowledgeStoreContainer.shared.write(.gamePage(entry))
    Worker (Plan 02) now applies fenced-section merge for games/ paths, so existing agent-authored content is preserved.

    Step 5: Rewire AgentTools `query_wiki` in `Sources/cellar/Core/Tools/ResearchTools.swift`.

    Replace:
        let result = await WikiService.search(query: query)
        return result
    with:
        let filter = KnowledgeListFilter(slug: slugify(query), maxResults: 5)
        let metas = await KnowledgeStoreContainer.shared.list(filter: filter)
        let env = EnvironmentFingerprint.current(...)
        let context = await KnowledgeStoreContainer.shared.fetchContext(for: query, environment: env) ?? ""
        return formatQueryWikiResult(metas: metas, context: context, query: query)
    Build a small `formatQueryWikiResult` helper (file-scope private) that returns a non-nil String human-readable result. Preserve the no-match contract (always return String, never nil).

    Step 6: Audit `Sources/cellar/Core/Tools/SaveTools.swift`.

    save_success and save_failure tool implementations are not in the AIService memory call sites being rewired (per RESEARCH.md they remain tools in AgentTools; AIService calls KnowledgeStore.write post-loop). VERIFY by reading the file: if SaveTools currently calls into WikiService.postSessionLog directly, route it through KnowledgeStoreContainer.shared.write(.sessionLog(...)) too. Otherwise leave as-is.

    Step 7: Extend integration tests.

    In `Tests/cellarTests/KnowledgeStoreIntegrationTests.swift`, add tests #1-6 from Behavior. Each test swaps KnowledgeStoreContainer.shared with the StubKnowledgeStore actor and asserts the wrapper delegates. Reset the singleton in teardown.

    Step 8: Verify NOTHING outside the wrapper transition layer references KnowledgeStoreLocal. Plan 03's KnowledgeStoreLocal is for offline/test use only — its activation is deferred to a follow-up phase. The current production wiring uses KnowledgeStoreRemote unconditionally.
  </action>
  <verify>
    <automated>cd /Users/peter/Documents/Cellar && swift build 2>&1 | tail -30 && swift test 2>&1 | tail -40 && grep -rn "KnowledgeStoreContainer.shared" Sources/cellar/Core/ | wc -l</automated>
  </verify>
  <done>All four legacy services are now thin wrappers; query_wiki tool routes through the store; WikiIngestService writes via /api/knowledge/write (fenced merge active); SaveTools verified; integration tests pass; full test suite green; ≥10 references to KnowledgeStoreContainer.shared in Sources/cellar/Core/ confirms wide adoption.</done>
</task>

</tasks>

<verification>
- `swift build` succeeds with no warnings
- `swift test` runs the full suite — all existing tests + new integration tests pass
- `grep -rn "agentValidWinetricksVerbs" Sources/ Tests/` returns 0 matches (declaration deleted, all callers migrated)
- `grep -rn "KnowledgeStoreContainer.shared" Sources/cellar/Core/` returns ≥10 hits across AIService + the four wrappers + ResearchTools + SaveTools
- `grep -n "WikiService.postSessionLog\|WikiService.postFailureSessionLog\|CollectiveMemoryWriteService.push" Sources/cellar/Core/AIService.swift` returns 0 matches (call sites migrated)
- Manual smoke test (deferred to phase verification): run `cellar serve`, launch a game, confirm context is built and a session log appears in the cellar-memory repo with no breakage
- Manual smoke test 2: run `cellar wiki ingest <game>`, then re-run with the same game; verify the second run preserves any agent-authored content outside the AUTO fence
</verification>

<success_criteria>
- KnowledgeStoreContainer.shared is the single live data path for all game knowledge reads/writes
- The four legacy services exist only as thin wrappers (≤30 lines of body each, excluding shared helpers used by adapters)
- Allowlists exist in exactly one place (PolicyResources / Resources/policy/*.json)
- Worker fenced-section merge is exercised end-to-end via WikiIngestService
- All 186+ existing tests pass; new integration tests demonstrate wrapper-to-store delegation
</success_criteria>

<output>
After completion, create `.planning/phases/44-collapse-memory-layer-into-single-knowledgestore-with-one-schema-and-local-plus-remote-adapters/44-04-SUMMARY.md` capturing:
- Final shape of each thin wrapper (signatures preserved, body shrunk)
- Where KnowledgeStoreContainer.shared is initialized at startup (filename + line)
- Final grep counts (`agentValidWinetricksVerbs` → 0, `KnowledgeStoreContainer.shared` → N)
- Any helpers extracted from legacy services (which file owns each helper now)
- Smoke test results: was a real session run end-to-end? Did `cellar wiki ingest` exercise the fenced merge?
- Notes for the future deletion phase: which legacy types are now safe to delete, and which still have unique helpers that need migrating first
</output>
