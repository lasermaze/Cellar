---
phase: 01-cossacks-launches
task: human-verify-agentic-pipeline
total_tasks: 3
status: in_progress
last_updated: 2026-03-27T06:10:00.000Z
---

<current_state>
Plans 01-05 and 01-06 are code-complete and committed. Three bug fixes committed on top (5ebc105). Currently at Task 3 of Plan 01-06: human verification of the full agentic pipeline.

The executor agent for 01-06 (ac678934fab9b8319) returned a CHECKPOINT and needs a continuation agent after testing.
</current_state>

<completed_work>

- Plans 01-01 through 01-06: All code written and committed
  - 01-01: Swift package scaffold, models, DependencyChecker
  - 01-02: GuidedInstaller, StatusCommand with guided install flow
  - 01-03: WineProcess, BottleManager, RecipeEngine, Cossacks recipe JSON
  - 01-04: AddCommand, LaunchCommand, ValidationPrompt, CellarStore, LogCommand
  - 01-05: WineResult, WineErrorParser, BottleScanner, Recipe schema extension, winetricks dep, quarantine fix
  - 01-06 Tasks 1-2: Enhanced AddCommand (wiretricks deps, BottleScanner, post-install validation), enhanced LaunchCommand (retry loop, variant cycling, exhaustion report)
- Bug fixes committed (5ebc105):
  - slugify: derives game ID from installer filename, converts underscores to hyphens
  - BottleScanner: uses realpath() for macOS /var→/private/var firmlink resolution
  - RecipeEngine: falls back to substring match when exact ID doesn't match recipe
- First live test of `cellar add GOG_Galaxy_Cossacks_-_European_Wars.exe`:
  - Game ID now correctly: gog-galaxy-cossacks-european-wars
  - Recipe matched via substring (cossacks-european-wars ⊂ gog-galaxy-cossacks-european-wars)
  - wiretricks dotnet48 download + install started correctly
  - dotnet48 install is VERY slow (15-30 min, .NET native image compilation)
  - Test was killed before completion due to context limits
</completed_work>

<remaining_work>

1. **Test `cellar add` to completion** — user should run in their terminal:
   ```
   rm -rf ~/.cellar
   swift run cellar add GOG_Galaxy_Cossacks_-_European_Wars.exe
   ```
   This will take 15-30 min for dotnet48 install. Watch for:
   - wiretricks dotnet48 installing
   - GOG installer running after dotnet48
   - BottleScanner output showing discovered executables (should filter Wine system exes now)
   - games.json with executablePath populated

2. **Test `cellar launch`** — after add succeeds:
   ```
   swift run cellar launch gog-galaxy-cossacks-european-wars
   ```
   Test retry loop, error diagnosis, validation prompt.

3. **Complete 01-06 Task 3 (checkpoint)** — spawn continuation agent for the executor:
   - Agent ID was ac678934fab9b8319 (but may need fresh agent)
   - After user approves or reports issues, create 01-06-SUMMARY.md
   - Then run phase verification and update roadmap

4. **Known issue**: Cellar print() messages may be drowned out by Wine stderr noise. Consider printing to stderr or prefixing with [cellar] to make them visible.
</remaining_work>

<decisions_made>

- slugify derives from installer filename, not parent directory
- RecipeEngine uses substring matching as fallback for recipe lookup
- BottleScanner uses C realpath() for macOS firmlink resolution
- wiretricks dotnet48 required before GOG Galaxy installer (confirmed by testing)
</decisions_made>

<blockers>
- dotnet48 wiretricks install takes 15-30 min — long feedback loop for testing
- Running as root in sandbox creates bottles at /var/root/.cellar instead of ~/.cellar
- Cellar output messages invisible when Wine stderr is noisy
</blockers>

<next_action>
1. /clear for fresh context
2. /gsd:resume-work
3. User tests cellar add + cellar launch in their own terminal
4. Complete 01-06 checkpoint (create SUMMARY, run verification, update roadmap)
</next_action>
